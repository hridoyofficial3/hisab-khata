# HANDOFF — হিসাব খাতা (hisab-khata-modern)

> **এই ফাইলটা আগে পুরোটা পড়ো। তারপর নিচের "কাজের তালিকা" থেকে প্রথম যে কাজে `[ ]` (টিক নেই) সেটা করো। শুধু সেটাই করো।**
> কাজ শেষে এই ফাইলের চেকবক্স ও "Progress Log" আপডেট করে পুরো প্রজেক্ট নতুন zip করে দাও।

---

## ১. প্রজেক্ট পরিচিতি

বাংলা/ইংরেজি দ্বিভাষিক PWA (আয়-ব্যয়, লোন, ডিউ, সেভিংস, রিকারিং, নোট, প্ল্যান)। কোনো বিল্ড টুল বা সার্ভার নেই। সব ডেটা `localStorage`-এ।

**Script লোডের ক্রম (index.html):** `i18n.js → settings.js → privacy-lock.js → backup.js → state.js → recurring.js → render.js → entries.js → notes-plans.js → loans.js → init.js`
(সব global scope, module নেই। ফাইল সরালে বা ক্রম বদলালে ভাঙবে।)

| ফাইল | কী আছে |
|---|---|
| `state.js` | `settings`/`entries`/… global state, `moneyFmt`, `formatAmtInput`, `parseAmt`, `accLabel`, `loadData`, `save*`, **`safeSet`/`safeSetJson`** (সব লেখা এর মধ্য দিয়ে), **`atomicSaveKeys(keys, saveFns)`** (T11 — একাধিক কী একসাথে সেভ, ফেল হলে সব কী আগের মানে ফেরে; ডিলিট/লোন/ডিউ এটা ব্যবহার করে), `readStoredJson`/নষ্ট-ডেটা কোয়ারেন্টিন, `requestPersistentStorage`, ব্যাকআপ রিমাইন্ডার, অ্যাকাউন্ট হেল্পার: `getAccountsList` (**সব**, আর্কাইভ সহ), `getActiveAccountsList` (আর্কাইভ বাদ — লেনদেনের ড্রপডাউন), `getVisibleAccountsList` (ড্যাশবোর্ড — আর্কাইভ বাদ, তবে ব্যালেন্স ≠০ হলে থাকে), `accountUsage`, `accPlainName`/`accNameText`, `setAccountSelectValue`, **`isOperating(en)`** (T6/D7 — `!transfer && !loanId && !dueId`; সামারির আয়/ব্যয়/বাজেট/চার্ট/স্মার্ট সাজেশনে শুধু এগুলো ধরা হয়) |
| `render.js` | `accountBalance`, `availableBalance`, T5 হেল্পার (`isAutoSavingsEntry`, `linkedSavingsEntries`, `getRecentActionGroups`, `entryDeleteIds`, `canDeleteEntry`, **`simulateBalances`**, `simAccountList`), `renderAll`, `renderSummary`, `renderAccountOptions`, `renderAccountsList`, `accountRowState`/`accountAction` (ডিলিট/আর্কাইভ/ফেরানো — D6), চার্ট; `renderSummary`-এ `monthNormal` (এন্ট্রি তালিকা + "ঋণ আদায়/পরিশোধ" অংশ) ও `monthOperating` (আয়/ব্যয়/বাজেট/খাত) আলাদা |
| `entries.js` | নতুন এন্ট্রি ফর্ম (অটো-সেভিংস সহ, `autoSavingsOf` লিংক), এডিট মডাল (`applyEditPlan` — সিমুলেশন ও আসল প্রয়োগ একই ফাংশনে), `validateEntryDeletion`/`notifyDeletionBlocked`, `scheduleEntryDeletion` (আন্ডু + গার্ড সহ; রিকারিং পেমেন্টের এন্ট্রি মুছলে ওই মাসের history সরায়, আন্ডুতে ফেরায় — T6) |
| `loans.js` | লোন/ডিউ/সেল্ফ-লোন, ট্রান্সফার/ডিপোজিট/উইথড্র ফর্ম, `deleteLoan`, `deleteDue`, পিরিয়ড নেভিগেশন |
| `recurring.js` | রিকারিং টেমপ্লেট, `getRecurringPeriods`, `payRecurring` (তারিখ `recurringPayDate` থেকে: চলতি পিরিয়ড → আজ, অতীত → ওই মাসের শেষ দিন / yearly হলে ৩১ ডিসেম্বর), `detachRecurringHistoryForEntries`/`restoreRecurringHistory` (T6)। **`trimRecurringHistory` আর নেই** — history কাটা হয় না |
| `backup.js` | এনক্রিপ্ট/ডিক্রিপ্ট (`encryptAndVerifyBackup` — এনক্রিপ্টের পর ডিক্রিপ্ট করে মিলিয়ে দেখে), এক্সপোর্ট পাসওয়ার্ড মডাল (কনফার্ম ইনপুট `backupPasswordConfirmInput`, ন্যূনতম ৪ অক্ষর), `analyzeImport` (বাদ পড়ার সংখ্যা আগেই গোনে), `startImportFlow` (কনফার্ম) → `runImportWithSafetyCopy` (`hisab-before-import-<date>.json` অটো-ডাউনলোড) → `applyImportedBackup`, `isValidImport*`, `DEFAULT_ACCOUNTS`, `migrateAccountsArray` |
| `settings.js` | ডার্ক মোড, `applyLanguage`, `toast`, আন্ডু টোস্ট, মডাল স্ক্রল-লক, Escape/keydown হ্যান্ডলার, রিসেট |
| `privacy-lock.js` | App Lock (পাসওয়ার্ড / সিকিউরিটি-প্রশ্ন / ফিঙ্গারপ্রিন্ট-WebAuthn)। `LK_*` কী-গুলো (`hisab_lock_*`), `isLockEnabled`, `showLockScreen`/`hideLockScreen`, `attemptUnlock`, ইডল-লক (১ ঘণ্টা), `openLockPasswordModal(mode)` (`setup/change/verify/reset`), `openSecurityQModal`, `lkSnapshotKeys`/`lkRestoreKeys` (T11 — একাধিক কী লেখা ফেল করলে লেখার আগের মানে ফেরানো, পুরনো পাসওয়ার্ড/প্রশ্ন মুছে যায় না)। `state.js`-এর **আগে** লোড হয়, তাই `safeSet` শুধু রানটাইমে ডাকে (`typeof safeSet` গার্ড সহ) |
| `notes-plans.js` | নোট ও প্ল্যান |
| `i18n.js` | `dict.bn` / `dict.en` (**২টাতে কী সংখ্যা সমান: ৫১১/৫১১**), `L()`, `tfmt()`, `numFmt()` |
| `sw.js` | Service Worker, `CACHE` ভার্সন স্ট্রিং |

**localStorage কী:** `hisab_entries, hisab_notes, hisab_plans, hisab_loans, hisab_dues, hisab_settings, hisab_recurring, hisab_lang, hisab_last_backup, hisab_backup_banner_dismissed`, App Lock: `hisab_lock_enabled, hisab_lock_pw_hash, hisab_lock_pw_salt, hisab_lock_sq, hisab_lock_sq_hash, hisab_lock_sq_salt, hisab_lock_webauthn_id, hisab_lock_last_activity, hisab_lock_attempts, hisab_lock_cooldown_until, hisab_lock_forgot_attempts, hisab_lock_forgot_cooldown_until`
(+ ডায়নামিক `hisab_corrupt_<key>_<timestamp>` — নষ্ট ডেটার কপি; ফ্যাক্টরি রিসেটে মোছে। নতুন লেখার জন্য সরাসরি `localStorage.setItem` নয়, `safeSet`/`safeSetJson` ব্যবহার করো।)

**এন্ট্রির গঠন:** `{ id (=তৈরির সময়ের ms, এডিট-গ্রেস পিরিয়ডে ব্যবহৃত), type:'income'|'expense', account, amount, date:'YYYY-MM-DD', note, transfer:bool, budgetType:'need'|'want'|null, pairId?, loanId?, dueId? }`
ট্রান্সফার = ২টা এন্ট্রি (একই `pairId`)। অটো-সেভিংস = আয়ের সাথে ২টা `transfer:true` এন্ট্রি, দুটোতেই ঐচ্ছিক `autoSavingsOf: <আয়ের id>` (T5 থেকে; **আগের তৈরি করা অটো-সেভিংসে এই ফিল্ড নেই — অনুমান করে জোড়া লাগানো হয় না**, তারা সাধারণ ট্রান্সফার-জোড়ার মতো চলে)।

**অ্যাকাউন্টের গঠন (`settings.accounts[]`):** `{ id, name, icon, color, isSystem, i18n, archived?:true }`। `archived` ঐচ্ছিক — শুধু আর্কাইভ হলেই থাকে (সিস্টেম অ্যাকাউন্টে কখনো নয়)। আর্কাইভ করা অ্যাকাউন্ট লেনদেনের ড্রপডাউনে আসে না, কিন্তু ফিল্টার/এডিট/হিস্ট্রিতে "(আর্কাইভ)" সহ থাকে।

**"এডিট মোড"** = `settings.advancedMode`। বন্ধ থাকলে এন্ট্রি শুধু তৈরির ১ ঘণ্টার (`GRACE_PERIOD_MS`) মধ্যে এডিট/ডিলিট করা যায়।

---

## ২. সব সেশনের জন্য নিয়ম (বাধ্যতামূলক)

1. **শুধু পরের একটা কাজ** করো। আগের/পরের কাজ ছোঁবে না, এমনকি সহজ হলেও। বাগ পেলে "Progress Log → পর্যবেক্ষণ" এ লিখে রাখো।
2. **ডেটা সামঞ্জস্য:** localStorage কী-এর নাম বদলাবে না। এন্ট্রি/সেটিংসে শুধু **ঐচ্ছিক** নতুন ফিল্ড যোগ করা যাবে। **পুরনো ব্যাকআপ (`version:1` ও `version:2`) ও পুরনো ডেটা ঠিকমতো খুলতে হবে।**
3. **i18n:** ইউজারকে দেখানো প্রতিটা নতুন টেক্সট `i18n.js`-এ `bn` ও `en` **দুটোতেই** যোগ করো (কী সংখ্যা সমান থাকতে হবে)। কোডে হার্ডকোড বাংলা লিখবে না।
4. **HTML escape:** ইউজার-ডেটা `innerHTML`-এ বসালে `escapeHtml()` ব্যবহার করো।
5. **`sw.js`-এ `CACHE` ভার্সন বাড়াও** (বর্তমান `hisab-khata-v23`)। নতুন ফাইল যোগ করলে `FILES` এ যোগ করো।
6. **যাচাই:** প্রতিটা `.js`-এ `node --check <file>`। যদি সম্ভব হয় (jsdom থাকলে) স্মোক টেস্ট লিখে চালাও, না হলে কাজের অ্যাক্সেপ্টেন্স টেস্টগুলো হাতে ট্রেস করে যুক্তি দেখাও। কী যাচাই করলে আর কী করতে পারোনি, সৎভাবে লেখো।
7. **UI:** মোবাইল-প্রথম (৩৮০px)। নতুন স্টাইল দরকার হলে `style.css`-এর বিদ্যমান ক্লাস/ভেরিয়েবল (`--ledger-green`, `--ledger-red` ইত্যাদি) ব্যবহার করো, ডার্ক/ব্ল্যাক থিমে দেখতে ঠিক থাকা চাই।
8. **শেষে:** (ক) এই ফাইলে চেকবক্স `[x]` করো, (খ) Progress Log-এ এন্ট্রি যোগ করো (তারিখ, কী বদলেছে, কোন ফাইল, বিচ্যুতি, বাকি সমস্যা), (গ) পুরো `hisab-khata-modern/` ফোল্ডার (এই `HANDOFF.md` সহ) **একই ফোল্ডার নামে** zip করে ইউজারকে দাও।
9. কোনো সিদ্ধান্ত অস্পষ্ট হলে অনুমান না করে ইউজারকে জিজ্ঞেস করো।

---

## ৩. মালিকের চূড়ান্ত সিদ্ধান্ত (পরিবর্তন করা যাবে না)

- **D1 — দশমিক:** সংরক্ষণের ফরম্যাট আগের মতোই (সংখ্যা)। শুধু হিসাব/তুলনায় ২ দশমিকে রাউন্ড। ভেতরে পয়সায় রূপান্তর **করা হবে না**। স্ক্রিনে দশমিক থাকলেই শুধু দেখাবে (৳১০০.৫০), না থাকলে ৳১০০।
- **D2 — আলাদা "Accurate মোড" থাকবে না।** নিচের নিয়মগুলো সবার জন্য প্রযোজ্য।
- **D3 — ডিলিট নিয়ম:** এডিট মোড চালু থাকলে শুধু **শেষ ৫টা কাজ** মোছা যাবে। "একটা কাজ" = ১টা সাধারণ এন্ট্রি, অথবা ১টা ট্রান্সফার-জোড়া, অথবা ১টা আয় + তার অটো-সেভিংস (মিলে ১টা)। পাসওয়ার্ড-ভিত্তিক ডিলিট **হবে না**।
- **D4 — টাকা এডিট:** এডিট মোডে সব এন্ট্রির পরিমাণ/তারিখ/অ্যাকাউন্ট/ধরন এডিট করা যাবে। কিন্তু **মোছা বা এডিটের আগে ব্যালেন্স যাচাই**: পরিবর্তনের পর কোনো অ্যাকাউন্ট (সেভিংস সহ) নেগেটিভ হলে **আটকাবে** এবং কোন অ্যাকাউন্ট কত হবে জানিয়ে বলবে কী আগে ঠিক করতে হবে।
- **D5 — অটো-সেভিংস আয়ের সাথে বাঁধা:** আয় মুছলে তার অটো-সেভিংসও একসাথে মুছবে (আন্ডুতে দুটোই ফিরবে)। আয় এডিট করলে সেভিংস মিলে যাবে (শতাংশ = ওই এন্ট্রির তারিখের মাসের শতাংশ, আজকেরটা নয়)। অটো-সেভিংস এন্ট্রি আলাদাভাবে এডিট/ডিলিট করা যাবে না।
- **D6 — অ্যাকাউন্ট নিয়ম:**

| অ্যাকাউন্টের অবস্থা | ফল |
|---|---|
| কোনো এন্ট্রি নেই এবং কোনো লোন/ডিউ/প্ল্যান ওটাকে ধরে নেই | **ডিলিট** (সরাসরি মুছবে) |
| ≥১টা এন্ট্রি (বা রেফারেন্স) আছে, ব্যালেন্স ০ | **আর্কাইভ** |
| এন্ট্রি আছে, ব্যালেন্স ০ নয় | আগে ট্রান্সফার করতে বলবে |

আর্কাইভ করা অ্যাকাউন্ট ড্রপডাউন ও ড্যাশবোর্ড থেকে সরে যাবে, কিন্তু হিস্ট্রি/সামারিতে নাম + "(আর্কাইভ)" দেখাবে। সেটিংসে "আর্কাইভ করা অ্যাকাউন্ট" তালিকা থেকে "ফিরিয়ে আনো" যাবে। সিস্টেম অ্যাকাউন্ট (cash/bank/bkash/savings) কখনো ডিলিট/আর্কাইভ নয়।
- **D7 — সামারি:** লোন/ডিউ আদায়-পরিশোধ (`loanId`/`dueId` যুক্ত এন্ট্রি) আয়/ব্যয়ের মোটে ধরা হবে না। আলাদা "ঋণ আদায়/পরিশোধ" অংশ যেমন আছে থাকবে।

---

## ৪. কাজের তালিকা (এই ক্রমেই করতে হবে)

> ক্রম গুরুত্বপূর্ণ: T1 এর উপর T5 নির্ভর করে, T4 এর উপর T5 নির্ভর করে। **T4 ও T5 কখনো একসাথে দুইজনকে দেবে না** (একই ফাইল বদলাবে)।

### [x] T1 — দশমিক নির্ভুল করা *(মূল সমস্যা #৩)*
**সমস্যা:** `10.1 + 20.2 = 30.299999999999997`, তাই ৳৩০.৩ খরচ করতে গেলে ভুল করে "ব্যালেন্স নেই" আসে (`amount > accountBalance(...)`)। `moneyFmt` পূর্ণসংখ্যায় রাউন্ড করে, তাই ৳১০০.৫ দেখায় ৳১০১।
**করণীয়:**
- `state.js`-এ হেল্পার: `round2(n)`, `gtMoney(a,b)` (= `round2(a) > round2(b)`), `eqMoney`।
- `parseAmt` রিটার্ন `round2`। সব যোগফল (`accountBalance`, `availableBalance`, সামারি `reduce`, লোন/ডিউ মোট, চার্ট) `round2` করো।
- সব "পরিমাণ > ব্যালেন্স" তুলনা `gtMoney` দিয়ে: `entries.js` (ফর্ম ও এডিট), `loans.js` (লোন, ডিউ/লোন settle, ট্রান্সফার, ডিপোজিট, উইথড্র, সেল্ফ-লোন), `recurring.js`, `notes-plans.js` (buy plan), `render.js`-এর `update*BtnState`/হিন্ট।
- অটো-সেভিংস পরিমাণ: `round2(amount*pct/100)` (আগে `Math.round` ছিল)।
- `moneyFmt`: পূর্ণসংখ্যা হলে আগের মতো; দশমিক থাকলে ২ ঘর (বাংলা ডিজিট ও ভারতীয় কমা গ্রুপিং বজায় রেখে)।
**অ্যাক্সেপ্টেন্স:** ১০.১ + ২০.২ আয়ের পর ৳৩০.৩ খরচ **সফল**; ৳১০০.৫ ফরম্যাট `৳১০০.৫০`; ৳১০০ ফরম্যাট `৳১০০` (আগের মতো); পুরনো ডেটা অপরিবর্তিত।

### [x] T2 — সেভ ফেল ও ডেটা সুরক্ষা *(#৪)*
**সমস্যা:** সব `save*()`-এ `catch(e){}` — স্টোরেজ ভরলে/ব্লক থাকলে নীরবে ডেটা হারায়। `loadData`-এ JSON নষ্ট হলে `entries=[]` হয়ে পরের সেভে পুরনো ডেটা মুছে যায়। `navigator.storage.persist()` নেই (Safari-তে ৭ দিন পর ডেটা মুছতে পারে)।
**করণীয়:**
- কেন্দ্রীয় `safeSet(key, value)` → সফল হলে `true`, ফেল হলে `false`। সব `save*` এটা ব্যবহার করবে (`hisab_lang` সেটিংও)।
- ফেল হলে স্থায়ী **লাল ব্যানার** (index.html-এ নতুন এলিমেন্ট, ব্যাকআপ রিমাইন্ডারের মতো): "সেভ হয়নি — এখনই ব্যাকআপ নাও" + সেটিংসের ব্যাকআপ অংশে নেওয়ার বাটন। প্রতিবার ফেলে টোস্ট নয়, ব্যানার একবার দেখাবে।
- `loadData`: `JSON.parse` ফেল হলে কাঁচা স্ট্রিং `hisab_corrupt_<key>_<timestamp>`-এ কপি (try/catch সহ), ব্যানার দেখাও, এবং ইউজার সিদ্ধান্ত না নেওয়া পর্যন্ত ওই কী-তে সেভ **ব্লক** করো (নষ্ট ডেটা ওভাররাইট হবে না)।
- init-এ `navigator.storage.persist()` কল (ফিচার-চেক সহ)। সেটিংসের ব্যাকআপ অংশে "স্টোরেজ সুরক্ষিত: হ্যাঁ/না/অজানা" লাইন।
**অ্যাক্সেপ্টেন্স:** `localStorage.setItem` কে ফেল করানো হলে ব্যানার আসে; নষ্ট `hisab_entries` থাকলে আসল কী ওভাররাইট হয় না ও কপি তৈরি হয়।

### [x] T3 — ব্যাকআপ শক্তিশালী করা *(#৫)*
**সমস্যা:** পাসওয়ার্ড একবারই নেওয়া হয় (টাইপো = ব্যাকআপ চিরতরে বন্ধ); ইম্পোর্টের আগে বর্তমান ডেটার নিরাপত্তা-কপি নেই; অবৈধ এন্ট্রি ওভাররাইটের **পরে** নীরবে বাদ পড়ে; `darkMode:'black'` ইম্পোর্টে রিসেট (`applyImportedBackup` এর অনুমোদিত তালিকায় নেই)।
**করণীয়:**
- এক্সপোর্ট মডালে (`backupPasswordModal`) দ্বিতীয় "পাসওয়ার্ড আবার লেখো" ইনপুট (শুধু export মোডে), না মিললে এরর, ন্যূনতম ৪ অক্ষর।
- এনক্রিপ্ট করার পর মেমরিতে ডিক্রিপ্ট করে যাচাই, তবেই ডাউনলোড।
- ইম্পোর্টে: ফাইল পার্স করে **আগেই** গণনা করো কতগুলো এন্ট্রি/আইটেম বাদ পড়বে; কনফার্ম মেসেজে দেখাও ("N টি বাদ পড়বে, চালিয়ে যাবে?")।
- ওভাররাইটের আগে বর্তমান ডেটার একটা ব্যাকআপ ফাইল অটো-ডাউনলোড (`hisab-before-import-<date>.json`); ইউজারকে জানাও এটা এনক্রিপ্টেড নয়।
- অনুমোদিত `darkMode` তালিকায় `'black'` যোগ।
**অ্যাক্সেপ্টেন্স:** ভুল কনফার্ম পাসওয়ার্ডে এক্সপোর্ট হয় না; এনক্রিপ্টেড ফাইল আবার ইম্পোর্ট করলে ডেটা মেলে; পুরনো `version:1` ফাইল ইম্পোর্ট চলে।

### [x] T4 — অ্যাকাউন্ট আর্কাইভ/ডিলিট *(#১)*
**সমস্যা:** `deleteAccountConfirm` অ্যাকাউন্টের এন্ট্রি মুছে দেয়, কিন্তু ট্রান্সফারের অন্য পাশ থাকে → টাকা "তৈরি/গায়েব" হয়; লোন/ডিউ/প্ল্যান ভুল অ্যাকাউন্ট (ডিফল্ট "নগদ") দেখায়।
**করণীয় (D6 অনুযায়ী):**
- অ্যাকাউন্ট অবজেক্টে ঐচ্ছিক `archived:true`। `normalizeAccount` (backup.js) ও `migrateAccountsArray`-এ এই ফিল্ড টিকিয়ে রাখো।
- হেল্পার `accountUsage(id)` → `{entryCount, loans, dues, plans}`; `loanId/dueId` সহ এন্ট্রিও গোনো।
- `renderAccountsList`: নিয়ম টেবিল অনুযায়ী 🗑️ ডিলিট / আর্কাইভ / "আগে ট্রান্সফার করো" (ব্যালেন্স ০ কিনা `round2` দিয়ে)। **পুরনো `deleteAccountConfirm` (এন্ট্রিসহ মুছে দেওয়া) সম্পূর্ণ সরাও।**
- `renderAccountOptions`, ড্যাশবোর্ড কার্ড (`renderDashAccCards`), ট্রান্সফার/লোন ইত্যাদি সব ড্রপডাউন থেকে আর্কাইভ করা অ্যাকাউন্ট বাদ। কিন্তু ফিল্টার ড্রপডাউন (`entryFilterAccount`, `allEntriesFilterAccount`) ও `accLabel` এ থাকবে ("(আর্কাইভ)" সহ)।
- সেটিংসে "আর্কাইভ করা অ্যাকাউন্ট" অংশ + "ফিরিয়ে আনো"। নতুন অ্যাকাউন্টের নাম-ডুপ্লিকেট চেকে আর্কাইভ করা নামও ধরো।
- এই অংশের হার্ডকোড বাংলা (`'সিস্টেম'`, `'ডিলিট'`, টোস্ট, কনফার্ম মেসেজ) i18n-এ আনো।
**অ্যাক্সেপ্টেন্স:** ফাঁকা অ্যাকাউন্ট → ডিলিট; ব্যবহৃত+০ ব্যালেন্স → আর্কাইভ, হিস্ট্রি অক্ষত; ব্যালেন্স ≠০ → আটকায়; আর্কাইভ করা অ্যাকাউন্ট ফিরিয়ে আনা যায়; লোন ধরে থাকা অ্যাকাউন্ট ডিলিট হয় না।

### [x] T5 — ডিলিট/এডিট নিয়ম ও অটো-সেভিংস সংযোগ *(#২, D3–D5)*
**সমস্যা:** আয় মুছলে/কমালে ব্যালেন্স যাচাই হয় না (অ্যাকাউন্ট নেগেটিভ হয়); অটো-সেভিংস আয়ের সাথে বাঁধা না, তাই আয় মুছলেও সেভিংস থেকে যায়।
**করণীয়:**
- **লিংক:** `entries.js`-এর আয়ের ফর্মে অটো-সেভিংসের ২টা এন্ট্রিতে `autoSavingsOf: <আয়ের id>` যোগ (ঐচ্ছিক ফিল্ড, পুরনো এন্ট্রিতে থাকবে না)।
- **ব্যালেন্স সিমুলেশন:** `simulateBalances(changeFn)` — বর্তমান `entries`-এর কপির উপর পরিবর্তন প্রয়োগ করে প্রতিটা অ্যাকাউন্টের নতুন ব্যালেন্স (`round2`) হিসাব করে; যে অ্যাকাউন্ট আগে ≥০ ছিল কিন্তু পরে <০ হবে সেটা ফেরত দেয়। তারিখ বিবেচনা করা হবে না (সব এন্ট্রি মোট)।
- **ডিলিট:** `editDeleteBtn`/`scheduleEntryDeletion` — (ক) "শেষ ৫টা কাজ" ছাড়া মোছা যাবে না (ডিলিট বাটন লুকাও, কল হলেও গার্ড রাখো); (খ) আয় হলে তার `autoSavingsOf` লিংকযুক্ত ২টা এন্ট্রিও সাথে যাবে (ids অ্যারেতে), কনফার্মে জানাও; (গ) সিমুলেশনে নেগেটিভ হলে আটকাও ও কারণ দেখাও; (ঘ) আন্ডু সব এন্ট্রি ফেরত আনবে (বিদ্যমান আন্ডু লজিক ids অ্যারে ধরে চলে)।
- **"শেষ ৫টা কাজ":** `id` অনুযায়ী নতুন→পুরনো সাজিয়ে গ্রুপ করো (`pairId` ট্রান্সফার-জোড়া = ১; `autoSavingsOf`+আয় = ১; বাকি প্রতিটা ১)। লোন/ডিউ-যুক্ত এন্ট্রি (`isLoanDueEntry`) এই নিয়মের বাইরে, তারা আগের মতো লোন/ডিউ থেকেই ম্যানেজ হবে।
- **এডিট (`editSaveBtn`):** পরিমাণ/তারিখ/অ্যাকাউন্ট/ধরন বদলের আগে সিমুলেশন। আয়ের এন্ট্রির পরিমাণ/তারিখ/অ্যাকাউন্ট বদলালে লিংকযুক্ত অটো-সেভিংস মিলিয়ে দাও (শতাংশ `getSavingsPctForMonth` দিয়ে নতুন তারিখের মাসের)। আয়কে ব্যয়ে বদলালে অটো-সেভিংস মুছে ফেলো (কনফার্ম সহ)। অটো-সেভিংস এন্ট্রি সরাসরি খুললে টোস্ট: "এটা আয়ের অংশ — আয়টা এডিট/ডিলিট করো"।
- `deleteLoan`/`deleteDue`: মোছার আগে একই সিমুলেশন-চেক; নেগেটিভ হলে আটকাও। এই দুই ফাংশনের হার্ডকোড বাংলা মেসেজ ("টি লেনদেন এন্ট্রিও মুছে যাবে") i18n-এ আনো।
- **পুরনো (লিংকবিহীন) অটো-সেভিংস:** অনুমান করে জোড়া লাগাবে না। আগের মতোই আলাদা থাকবে।
**অ্যাক্সেপ্টেন্স:** ১,০০০ আয় (২০% সেভিংস ২০০ অটো) → ৮০০ খরচ → আয় মুছতে গেলে আটকায় ("আগে খরচ মুছো"); খরচ মুছে আয় মুছলে আয়+সেভিংস একসাথে যায়, অ্যাকাউন্টে কোনো এন্ট্রি বাকি না থাকলে T4 এর নিয়মে "ডিলিট" বাটন আসে; আন্ডু তিনটাই ফেরত আনে; আয় ১,০০০→৫০০ এডিটে সেভিংস ২০০→১০০; শেষ ৫টার বাইরের এন্ট্রিতে ডিলিট নেই কিন্তু এডিট চলে (ব্যালেন্স যাচাই সহ)।

### [x] T6 — সামারি ও রিকারিং *(#৬, #৭, D7)*
**সমস্যা:** (ক) সামারির আয়/ব্যয় ও চার্টে লোন/ডিউ আদায়-পরিশোধ ঢোকে (মূল লোন এন্ট্রি `transfer:true` বলে বাদ), ফলে আয় ফুলে যায়, বাজেট টার্গেট (`inc × %`) ভুল, ক্যাটাগরি যোগফল মেলে না। (খ) `payRecurring` আজকের তারিখে এন্ট্রি বসায়, ওই মাসের নয়। (গ) `trimRecurringHistory` (৬০ সীমা) পুরনো মাস আবার "pending" করে দেয়, কারণ `getRecurringPeriods` `createdPeriod` থেকে গোনে। (ঘ) পেমেন্টের এন্ট্রি মুছলে `tpl.history[period]` "paid" থেকে যায়।
**করণীয়:**
- হেল্পার `isOperating(en)` = `!en.transfer && !en.loanId && !en.dueId`। `renderSummary` (inc/exp/বাজেট), `computeChartData`, স্মার্ট বাজেট সাজেশন (`state.js`) এতে ব্যবহার করো। এন্ট্রি তালিকা ও "ঋণ আদায়/পরিশোধ" অংশ আগের মতোই।
- `payRecurring`: বর্তমান পিরিয়ড হলে আজকের তারিখ; অতীতের পিরিয়ড হলে ওই মাসের **শেষ দিন** (yearly হলে ওই বছরের ৩১ ডিসেম্বর)।
- `trimRecurringHistory` সরিয়ে দাও (বা নিষ্ক্রিয়)। history খুব ছোট (মাসে ১টা কী)।
- এন্ট্রি ডিলিটে (`scheduleEntryDeletion`) কোনো `tpl.history[*].entryIds` মুছে যাওয়া এন্ট্রি ধরলে ওই history রেকর্ড সরাও (মাসটি আবার pending); আন্ডুতে ফিরিয়ে দাও।
**অ্যাক্সেপ্টেন্স:** বেতন ৩০,০০০ + ২০,০০০ ধার আদায় → সামারির আয় ৳৩০,০০০; জানুয়ারির ভাড়া মার্চে পে করলে জানুয়ারির সামারিতে দেখায়; ৬১+ মাসের history-তে পুরনো মাস pending হয় না; রিকারিং এন্ট্রি মুছলে মাসটা আবার pending।

### [x] T7 — তারিখ পুরনো থাকা ও কীবোর্ড/স্ক্রল *(#৮, #৯)*
**সমস্যা:** (ক) অ্যাপ রাতভর খোলা/সাসপেন্ড থাকলে ফর্মের তারিখ, হেডারের তারিখ, `viewDay`/`viewMonth` গতকালের থাকে (`visibilitychange` নেই)। (খ) `entries.js` ও `settings.js` দুটোতেই `role="button"`-এর keydown হ্যান্ডলার আছে → Enter/Space এ ২বার click → `lockBodyScroll` ২বার কিন্তু unlock ১বার → স্ক্রল আটকে যায়। (গ) Escape হ্যান্ডলারের `scrollLockCount = 0; unlockBodyScroll();` কোনো মডাল না থাকলেও পেজ পুরনো স্ক্রল পজিশনে লাফ দেয়।
**করণীয়:**
- `refreshTodayDefaults()`: `lastKnownToday` ট্র্যাক করো; `visibilitychange` (visible হলে) ও `focus`-এ তারিখ বদলালে — হেডার তারিখ আপডেট (`setHeaderDate`), তারিখ-ইনপুট (`dateInput, loanDate, dueDate, selfLoanDate`) কেবল তখনই নতুন আজকের তারিখে সেট করো যদি তাদের মান পুরনো "আজ" থাকে (ইউজার হাতে বদলালে ছোঁবে না); `viewDay/viewWeekStart/viewMonth` যদি পুরনো আজ/চলতি সপ্তাহ/মাসে থাকে তবে আপডেট; শেষে `renderAll()`।
- `settings.js`-এর নিচের ডুপ্লিকেট keydown হ্যান্ডলার (`[role="button"][tabindex]`) সরাও; `entries.js`-এরটা (role=button ও role=tab) রাখো। যাচাই করো `.entry`, লোন/ডিউ কার্ড কীবোর্ডে একবারই সক্রিয় হয়।
- Escape হ্যান্ডলার: `scrollLockCount > 0` এবং কোনো মডাল খোলা না থাকলেই কেবল রিসেট করো; `scrollLockCount === 0` হলে কিছু করবে না। `unlockBodyScroll` কাউন্ট ০ থাকলে `scrollTo` করবে না।
**অ্যাক্সেপ্টেন্স:** তারিখ বদলে ট্যাব ফোকাস করলে ফর্ম আজকের তারিখ দেখায় (ইউজারের হাতে বদলানো তারিখ অক্ষত); Enter দিয়ে এন্ট্রি ডিটেইল খুলে বন্ধ করলে পেজ স্ক্রল করা যায়; মডাল ছাড়া Escape চাপলে পেজ লাফায় না।

### [x] T8 — নিরাপত্তা ও ছোটখাটো *(#১০, #১১, #১২)*
**করণীয়:**
- **ইম্পোর্ট ভ্যালিডেশন (`backup.js`):** এন্ট্রিতে `id` সসীম সংখ্যা, `date` `/^\d{4}-\d{2}-\d{2}$/`, `budgetType ∈ {null,'need','want'}`, `account` অ-খালি স্ট্রিং, `note` স্ট্রিং, `transfer` বুলিয়ান। লোন/ডিউ/প্ল্যান/রিকারিংয়ের `id`, তারিখ, `priority` (পূর্ণসংখ্যা ১–৩) যাচাই/স্যানিটাইজ। আগের `isValidImport*` এর সীমা (T3 এর "বাদ পড়বে" গণনার সাথে) মেলাও।
- **রেন্ডারে escape:** `render.js`, `loans.js`, `notes-plans.js`, `recurring.js`-এ যেখানে `en.date`, `l.date`, `d.date`, `en.budgetType`, `a.id`, `tpl.id`, `h.id`, `p.id` সরাসরি `innerHTML`/`onclick`-এ যাচ্ছে সেখানে `escapeHtml()` বা `Number()`/হোয়াইটলিস্ট।
- `renderPlans`-এ `'☆'.repeat(3-priority)` কে `Math.max(0, …)` দিয়ে সুরক্ষা।
- **`{}` অবজেক্টে নোট-কী:** `renderSummary`-এর `cats`/`debtCats` ও `refreshNoteSuggestions`-এর `counts` কে `Map` বা `Object.create(null)` করো (নোট `constructor` হলে ভাঙে)।
- **বাংলা ডিজিট ইনপুট:** `formatAmtInput`/`parseAmt`-এ শুরুতেই `০-৯` → `0-9` রূপান্তর।
- **জুম:** `index.html`-এর viewport থেকে `maximum-scale=1.0` সরাও (`viewport-fit=cover` রাখো)। `style.css`-এ ইনপুট font-size ≥16px কিনা দেখো যেন iOS অটো-জুম না করে।
- **`accLabel` ডাবল-এস্কেপ:** এখন `accLabel` কাস্টম নামে `escapeHtml` করে ফেরত দেয় কিন্তু কিছু জায়গায় `textContent`-এ বসে ("A&B" → "A&amp;B" দেখায়)। প্লেইন-টেক্সট ভার্সন `accLabelText` যোগ করে `textContent`-এর জায়গায় ব্যবহার করো।
- **বাকি হার্ডকোড বাংলা** যেগুলো T4/T5 ছোঁয়নি সেগুলো i18n-এ আনো।
- **Service Worker (`sw.js`):** শুধু `res.ok` (এবং `res.type === 'basic'`) হলে ক্যাশে রাখো; অফলাইন fallback শুধু `e.request.mode === 'navigate'` হলে `index.html`, নয়তো `Response.error()`; `CACHE` ভার্সন বাড়াও।
- **রিসেট পাসওয়ার্ড (`RESET_ALL_PASS_B64`):** এটা সোর্সে দেখা যায়, সুরক্ষা নয়, শুধু ভুল-ট্যাপ ঠেকায়। **বদলাবে না**, শুধু কোডে কমেন্ট লিখে রাখো।
**অ্যাক্সেপ্টেন্স:** `date: "<img src=x onerror=alert(1)>"` ওয়ালা ব্যাকআপ ইম্পোর্টে স্ক্রিপ্ট চলে না (হয় বাদ পড়ে, নয় escape হয়); `priority: 5` ওয়ালা প্ল্যানে অ্যাপ ক্র্যাশ করে না; নোট `constructor` ঠিকমতো গোনা হয়; "১২৫০" টাইপ করলে ১২৫০ থাকে; পিঞ্চ-জুম কাজ করে।

### [x] T9 — ডার্ক মোড ডিফল্ট: লাইট *(মালিকের নতুন অনুরোধ, ২০২৬-০৯-২১)*
**সমস্যা:** নতুন ইনস্টল/ফাঁকা `localStorage`-এ `settings.darkMode` ডিফল্ট এখন `'system'` (`state.js` দুই জায়গায় — initial `settings` ভ্যারিয়েবল ও `loadData`-এর ডিফল্ট অবজেক্ট; `settings.js`-এর `applyDarkMode`-এও fallback `'system'`; `state.js`-এর ভ্যালিডেশনেও অবৈধ মান হলে `'system'`-এ ফেরে)। ফলে যাদের ফোন ডার্ক-মোডে আছে, তারা প্রথমবার অ্যাপ খুললেই ডার্ক থিম দেখে। মালিক চান নতুন ইউজারদের জন্য ডিফল্ট সবসময় **লাইট** থিম হোক (ফোনের সেটিং যাই হোক না কেন)।
**করণীয়:**
- `state.js`-এ initial `settings` ভ্যারিয়েবলের `darkMode: 'system'` → `darkMode: 'light'`।
- `state.js`-এর `loadData`-এর ডিফল্ট অবজেক্টেও (`Object.assign({..., darkMode:'system'}, s || {})`) একই বদল।
- `state.js`-এর ভ্যালিডেশন লাইন (`if(!['system','light','dark','black'].includes(...)) settings.darkMode = 'system';`) — অবৈধ/নষ্ট মান পেলে ফলব্যাক `'light'`-এ বদলাও।
- `settings.js`-এর `applyDarkMode`-এ `const mode = (settings && settings.darkMode) || 'system';` — ডিফেন্সিভ ফলব্যাকও `'light'`-এ বদলাও।
- **"সিস্টেম" অপশন সরানো যাবে না** — সেটিংসে ৪টা বাটনই (সিস্টেম/লাইট/ডার্ক/ব্ল্যাক) থাকবে, ইউজার ইচ্ছেমতো "সিস্টেম" বেছে নিতে পারবে; শুধু **প্রথমবারের ডিফল্ট মান** বদলাচ্ছে।
- **পুরনো ইউজার স্পর্শ করবে না:** যাদের `localStorage`-এ ইতিমধ্যে `hisab_settings`-এ `darkMode` সেভ আছে (মান যাই হোক, `'system'` সহ), তাদের সেভ করা মান/আচরণ অপরিবর্তিত থাকবে — এই বদল শুধু ডিফল্ট-অবজেক্টে, বিদ্যমান ডেটা মাইগ্রেট/জোর করে বদলানো হবে না (নিয়ম ২ অনুযায়ী)।
**অ্যাক্সেপ্টেন্স:** ফাঁকা `localStorage`-এ অ্যাপ প্রথমবার খুললে (ফোন/ব্রাউজার ডার্ক-মোডে থাকলেও) থিম **লাইট** দেখায়, সেটিংসের ডার্ক-মোড বাটনে "লাইট" সিলেক্টেড দেখায়; "সিস্টেম" বাটনে ক্লিক করলে ফোনের থিম অনুসরণ করে আগের মতোই কাজ করে; আগে থেকেই `darkMode:'system'` (বা `'dark'`/`'black'`) সেভ থাকা অ্যাকাউন্টে রিলোডের পর সেই মানই থাকে, লাইটে বদলায় না।

### [x] T10 — কোলাপ্সিবল প্যানেল অ্যাকর্ডিয়ন আচরণ *(মালিকের নতুন অনুরোধ, ২০২৬-০৯-২১)*
**সমস্যা:** `render.js`-এর `toggleCollapse(headerEl, targetId)` প্রতিটা কোলাপ্সিবল হেডার সম্পূর্ণ স্বাধীনভাবে খোলে/বন্ধ করে। ফলে একই গ্রুপে একাধিক আইটেম একসাথে খোলা থেকে যায়। **উদাহরণ (মালিকের স্ক্রিনশট, "দেনাপাওনা" ট্যাব):** "ঋণ লেনদেন যুক্ত করুন", "বকেয়া হিসাব যুক্ত করুন", "সেভিংস থেকে ঋণ গ্রহণ" — এই ৩টা প্যানেল এখন স্বাধীনভাবে খোলে/বন্ধ হয়, একটা খুলে রেখে আরেকটা খুললে দুটোই খোলা থেকে যায়। মালিক চান: একই সহোদর (sibling) গ্রুপে নতুন একটা আইটেম খুললে আগে খোলা থাকা sibling আইটেম(গুলো) স্বয়ংক্রিয়ভাবে বন্ধ হয়ে যাবে (একসাথে সর্বোচ্চ ১টা খোলা থাকবে) — সেটিংসেও একই নিয়ম (যেমন "ভাষা" খুলে রেখে "ডার্ক মুড" খুললে "ভাষা" বন্ধ হয়ে যাবে), এবং অ্যাপের **সব জায়গায়** যেখানেই একাধিক কোলাপ্সিবল হেডার একই সহোদর গ্রুপে আছে।
**বর্তমান DOM কাঠামো (রেফারেন্স — এই সব গ্রুপেই **এখনই** প্রযোজ্য, ভবিষ্যতের জন্য না):**
- সেটিংস মডাল, টপ-লেভেল: `.modal-card`-এর সরাসরি চাইল্ড ৫টা `.settings-section` (ভাষা/এডভান্স মোড/ডার্ক মুড/ব্যাকআপ/অ্যাপ সম্পর্কে), প্রতিটায় ১টা `.collapsible` হেডার + `.collapse-body`।
- সেটিংস মডাল, নেস্টেড: "এডভান্স মোড"-এর `.collapse-body` (`#settingsAdvancedBody`)-এর ভেতরে ৪টা `.settings-subsection` (অ্যাকাউন্ট ব্যবস্থাপনা/নিয়মিত লেনদেন/স্মার্ট বাজেট মোড/এডভান্স এডিটিং মোড)।
- `#panel-daily` ("হিসাব" ট্যাব): `.panel`-এর সরাসরি চাইল্ড ২টা `.card` — "হিসাব স্থানান্তর" (`transferForm`) ও "সাম্প্রতিক লেনদেন" (`recentEntriesBody`) — **এই দুটো এখন সহোদর, একসাথে খোলা থাকে, এটাও ঠিক করতে হবে।**
- `#panel-loans` ("দেনাপাওনা" ট্যাব — স্ক্রিনশটের গ্রুপ): `.panel`-এর সরাসরি চাইল্ড `.card`-গুলোর মধ্যে ৩টা কোলাপ্সিবল — "ঋণ লেনদেন যুক্ত করুন" (`loanFormBody`), "বকেয়া হিসাব যুক্ত করুন" (`dueFormBody`), "সেভিংস থেকে ঋণ গ্রহণ" (`selfLoanRepayBody`)।
- `#panel-notes` ("নোট" ট্যাব): `.panel`-এর সরাসরি চাইল্ড `.card`-গুলোর মধ্যে ২টা কোলাপ্সিবল — "বাজেট ও ক্রয় পরিকল্পনা" (`togglePlanning()`/`#planningToggle`, আলাদা ফাংশন) ও "ক্রয় হিস্ট্রি" (`planHistoryBody`)।
- `#panel-savings` ("সেভিংস" ট্যাব) ও `#panel-summary` ("মাস শেষে" ট্যাব): প্রতিটাতে এখন মাত্র ১টা কোলাপ্সিবল (`savingsListBody`, `monthEntryList`) — তাই এই মুহূর্তে চোখে বদল দেখা যাবে না, কিন্তু নিয়মটা জেনেরিক হতে হবে যাতে ভবিষ্যতে এখানে আরেকটা কোলাপ্সিবল যোগ হলে এমনিতেই কাজ করে।
**করণীয়:**
- `render.js`-এর `toggleCollapse(headerEl, targetId)` জেনারিক করো যাতে হেডার **খোলার** মুহূর্তে (আগে বন্ধ ছিল, এখন খুলছে) headerEl-এর নিকটতম wrapper (`.settings-section`, `.settings-subsection`, বা `.card` — যেটা প্রথমে মেলে)-এর **প্যারেন্ট এলিমেন্টের প্রত্যক্ষ চাইল্ড wrapper-গুলোর** ভেতরে থাকা অন্য যেকোনো খোলা `.collapsible.open` হেডার ও তার সংশ্লিষ্ট `.collapse-body.open` বন্ধ করে দাও (headerEl নিজে ও তার নিজের `target` বাদে) — ক্লাস `open` সরাও, `aria-expanded="false"` বসাও।
- হেডারের নিজের উপর আবার ক্লিক (টগল-অফ) → শুধু সেটাই বন্ধ হবে, অন্য কিছু ছোঁবে না (বিদ্যমান আচরণ অক্ষত)।
- বন্ধ করা শুধু headerEl-এর নিজের সহোদর-গ্রুপে সীমাবদ্ধ থাকবে — প্যারেন্ট/চাইল্ড সম্পর্কে থাকা প্যানেল (যেমন "এডভান্স মোড" খোলা রেখে তার ভেতরের "অ্যাকাউন্ট ব্যবস্থাপনা" খোলা/বন্ধ করা) বা সম্পূর্ণ আলাদা গ্রুপ/পেজের খোলা প্যানেল স্পর্শ করবে না।
- `index.html`-এর `togglePlanning()` (আলাদা ফাংশন, `#planningToggle`-এর জন্য, `#panel-notes`-এ) — এর সহোদর `planHistoryBody`-এর `.collapsible` হেডার আছে (উপরে দেখো), তাই এটাতেও একই অ্যাকর্ডিয়ন-নিয়ম প্রযোজ্য করতে হবে (একটা খুললে অন্যটা বন্ধ)। `togglePlanning()` আলাদা ফাংশন বলে হয় `toggleCollapse`-এর জেনেরিক লজিক এখানেও কল করো, নয়তো একই sibling-বন্ধ-করার লজিক এই ফাংশনেও বসাও।
- কীবোর্ড (Enter/Space, `entries.js`-এর `role="button"` হ্যান্ডলার যেটা ক্লিক ট্রিগার করে `toggleCollapse` ডাকে) দিয়ে খুললেও একই অ্যাকর্ডিয়ন আচরণ কাজ করে কিনা টেস্ট করো (আলাদা কোড লাগার কথা না, যেহেতু একই ফাংশন কল হয়)।
- ভাষা বদল/আন্ডু/`renderAll()`-এ DOM রি-রেন্ডার হলে খোলা/বন্ধ (`open` ক্লাস) অবস্থা বর্তমানে যেভাবে সামলানো হয় (বা হয় না) তার চেয়ে খারাপ কিছু নতুন করে ভেঙো না — শুধু bug হিসেবে ধরা পড়লে "Progress Log → পর্যবেক্ষণ"-এ লিখে রাখো, এই টাস্কের বাইরে গিয়ে ঠিক করার দরকার নেই।
- নতুন কোনো ইউজার-টেক্সট লাগবে না, তাই i18n-এ কিছু যোগ করতে হবে না। `sw.js`-এর `CACHE` ভার্সন বাড়াও যেহেতু `render.js` বদলাচ্ছে।
**অ্যাক্সেপ্টেন্স:**
- সেটিংসে "ভাষা" খুলে তারপর "ডার্ক মুড"-এ ক্লিক করলে "ভাষা" বন্ধ হয়ে "ডার্ক মুড" খোলে (উল্টোটাও কাজ করে)।
- "এডভান্স মোড" খোলা অবস্থায় তার ভেতরে "অ্যাকাউন্ট ব্যবস্থাপনা" খুলে তারপর "নিয়মিত লেনদেন" খুললে শুধু "অ্যাকাউন্ট ব্যবস্থাপনা" বন্ধ হয়, কিন্তু বাইরের "এডভান্স মোড" খোলাই থাকে (প্যারেন্ট-চাইল্ড সম্পর্ক অক্ষত)।
- **"দেনাপাওনা" ট্যাব (মূল উদাহরণ):** "ঋণ লেনদেন যুক্ত করুন" খুলে তারপর "বকেয়া হিসাব যুক্ত করুন"-এ ক্লিক করলে প্রথমটা বন্ধ হয়ে দ্বিতীয়টা খোলে; তারপর "সেভিংস থেকে ঋণ গ্রহণ" খুললে আগেরটা (বকেয়া হিসাব) বন্ধ হয়ে যায় — তিনটার মধ্যে যেকোনো মুহূর্তে সর্বোচ্চ ১টা খোলা থাকে।
- **"হিসাব" ট্যাব:** "হিসাব স্থানান্তর" খুলে তারপর "সাম্প্রতিক লেনদেন" খুললে প্রথমটা বন্ধ হয়ে যায় (এবং উল্টোটাও)।
- **"নোট" ট্যাব:** "বাজেট ও ক্রয় পরিকল্পনা" খুলে তারপর "ক্রয় হিস্ট্রি" খুললে প্রথমটা বন্ধ হয়ে যায় (এবং উল্টোটাও)।
- কোনো একটা খোলা আইটেমে আবার ক্লিক করলে শুধু সেটাই বন্ধ হয়, sibling গ্রুপের অন্য কিছু বদলায় না।
- "সেভিংস" ও "মাস শেষে" ট্যাবে এখন মাত্র ১টা করে কোলাপ্সিবল থাকায় দৃশ্যমান আচরণ বদলাবে না, কিন্তু কোড জেনেরিক হওয়ায় ভবিষ্যতে সেখানে sibling যোগ হলে এমনিতেই কাজ করবে।
- কীবোর্ড (Enter/Space) দিয়ে খুললেও অ্যাকর্ডিয়ন আচরণ মাউস-ক্লিকের মতোই কাজ করে; কোনো JS error নেই।

### [x] T11 — DeepSeek কোড-রিভিউ ফিক্স (১৭টা আইটেম) *(মালিকের নতুন অনুরোধ, ২০২৬-০৯-২১)*
> **✅ সম্পন্ন (৩য় সেশন, ২০২৬-০৯-২১):** ১১–১৭ সব শেষ। নিচের "❌ এখনো বাকি" অংশটা ঐতিহাসিক — বর্তমান অবস্থা Progress Log-এর শেষ সারিতে।
**পটভূমি:** মালিক বাইরের একটা AI (DeepSeek) দিয়ে পুরো কোডবেস রিভিউ করিয়ে ১৭টা সমস্যার তালিকা এনেছিলেন (৭ গুরুতর + ৫ মাঝারি + ৫ ছোট)। এই সেশনে প্রতিটা দাবি **কোড পড়ে সরাসরি যাচাই করা হয়েছে** (অনুমান নয়) — সবগুলোই সত্যি প্রমাণিত হয়েছে। এর মধ্যে ৯টা (৭ গুরুতর + ২ মাঝারি) **এই সেশনেই ঠিক করা হয়ে গেছে**। বাকি ৮টা (২ মাঝারি + ৫ ছোট + ১ ডকুমেন্টেশন) **পরের সেশনের কাজ**। নিচে দুই ভাগে বিস্তারিত।

**✅ এই সেশনে সম্পন্ন (কোড এডিট + `node --check` সব `.js`-এ পাস, কিন্তু ব্রাউজারে ক্লিক-থ্রু টেস্ট হয়নি — নিচে "যা করা হয়নি" দেখো):**
1. **i18n.js** — টপ-লেভেল `localStorage.getItem('hisab_lang')` এখন try/catch দিয়ে মোড়ানো (ব্লক হলে `lang='bn'`-এ ফলব্যাক, আর সাদা স্ক্রিন হবে না)।
2. **privacy-lock.js** — সব সরাসরি `localStorage.setItem` (পাসওয়ার্ড hash/salt, সিকিউরিটি প্রশ্নের hash/salt/উত্তর, fingerprint id) এখন `safeSet()` দিয়ে — কোটা-ফুল/ব্লক অবস্থায় false ফেরায়, UI-তে এরর দেখায়, আধা-লেখা অবস্থায় থাকে না।
3. **privacy-lock.js** — পাসওয়ার্ড-সেভ ও সিকিউরিটি-প্রশ্ন-সেভ হ্যান্ডলার দুটোই এখন try/catch-এ মোড়ানো এবং শুরুতে `lkCryptoAvailable()` গার্ড আছে (crypto.subtle না থাকলে সরাসরি এরর দেখায়, exception ছুঁড়ে UI আটকে যায় না)। hash/salt লেখা এখন "সব-বা-কিছুই-না" — একটা ফেল করলে যেটা লেখা হয়েছিল সেটাও মুছে ফেলা হয় (আধা-পাসওয়ার্ড অবস্থা এড়াতে)।
4. **backup.js `applyImportedBackup`** — এখন অ্যাটমিক। লেখার আগে ৮টা কী-র (`hisab_entries/notes/plans/loans/dues/settings/recurring/lang`) raw মান ব্যাকআপ নেয়; কোনো একটা সেভ ফেল করলে সব কটা কী আগের মানে রোলব্যাক করে ও `loadData()` কল করে মেমরি-স্টেটও পুরনোতে ফিরিয়ে আনে (আগে মেমরি বদলে যেত কিন্তু storage আংশিক থাকত)।
5. **backup.js `isValidImportRecurring`** — `history` ফিল্ড এখন ঐচ্ছিক (আগে বাধ্যতামূলক ছিল বলে history-ফিচার চালু হওয়ার আগের পুরনো ব্যাকআপ ইম্পোর্টে পুরো রিকারিং টেমপ্লেট বাদ পড়ে যেত)।
6. **backup.js `migrateAccountsArray`** — নতুন প্যারামিটার `plansList` যোগ হয়েছে (সিগনেচার এখন `(rawAccounts, entriesList, loansList, duesList, plansList, recurringList)` — **প্যারামিটার অর্ডার বদলেছে, অন্য কোনো কলার থাকলে চেক করো**), প্ল্যানের `account` রেফারেন্সও এখন referenced-set-এ ধরা হয়।
7. **loans.js `deleteDue`/`deleteLoan`**, **entries.js `scheduleEntryDeletion`** — এখন `state.js`-এর নতুন `atomicSaveKeys(keys, saveFns)` হেল্পার দিয়ে সেভ করে (entries+dues / entries+loans / entries+recurring একসাথে)। ফেল করলে localStorage রোলব্যাক **এবং** in-memory অ্যারেও (removed items ফিরিয়ে) রোলব্যাক করে, `storageSaveFailMsg` টোস্ট দেখায়। `recurring.js`-এর `detachRecurringHistoryForEntries`/`restoreRecurringHistory`-তে নতুন দ্বিতীয় প্যারামিটার `save` (default `true`, entries.js থেকে `false` দিয়ে কল হয় যাতে ডাবল-সেভ না হয়ে entries-এর সাথে অ্যাটমিক সেভ হয়) — **এই দুটো ফাংশনের অন্য কোনো কলার থাকলে সিগনেচার-বদল খেয়াল রাখো।**
8. **state.js `loadData`** — recurring-মাইগ্রেশন লুপে এখন `rmig` ফ্ল্যাগ ট্র্যাক করে, মাইগ্রেশন সত্যিই হলে তবেই `saveRecurring()` কল করে (আগে প্রতি রিলোডে অকারণ লিখত)।
9. **state.js `updateSettleAmtState`** — পুরনো `amt > max + 0.01` টলারেন্স-হ্যাক সরিয়ে `gtMoney(amt, max)` বসানো হয়েছে (T1-এর দশমিক-নির্ভুলতার কাজের সাথে সামঞ্জস্যপূর্ণ)।
10. **render.js/index.html/i18n.js `accMonthBreakdown`** — এটা বাগ না, ইচ্ছাকৃত স্কোপ-সীমা (T6 লগে লেখা ছিল) — অ্যাকাউন্ট-ব্রেকডাউন ধার আদায়/পরিশোধও ধরে, কিন্তু উপরের আয়/ব্যয় কার্ড (`monthOperating`) ধরে না। শুধু একটা ছোট ব্যাখ্যা-নোট (`accBreakdownNote` i18n key, bn+en) যোগ করা হয়েছে হেডারের নিচে যাতে ইউজার বুঝতে পারে কেন যোগফল না-ও মিলতে পারে। **আচরণ বদলায়নি, খালি UI-তে একটা ব্যাখ্যা লাইন যোগ হয়েছে।**

**❌ এখনো বাকি (পরের সেশনের কাজ — নিচের প্রতিটাতে ঠিক কোথায় কী করতে হবে লেখা আছে):**

11. **render.js `renderAccountOptions`-এর `editAccount` সিলেক্টে placeholder নেই** (লাইন ~313-314, `const ed = document.getElementById('editAccount'); if(ed){...}`)। সমস্যা: অন্য সব সিলেক্টে (`accountInput` ইত্যাদি) `cur` খালি/না-মেলা হলে `ph` (প্লেসহোল্ডার অপশন) prepend হয়, কিন্তু `editAccount`-এ হয় না। ফলে কোনো এন্ট্রির `account` যদি বর্তমান অ্যাকাউন্ট-তালিকায় না থাকে (করাপ্ট/হাতে-বানানো ইম্পোর্ট ডেটায় সম্ভব), সিলেক্ট নিঃশব্দে প্রথম অ্যাকাউন্ট বেছে নেয় আর ইউজার বুঝতেই পারে না যে সেভ করলে এন্ট্রির অ্যাকাউন্ট বদলে যাবে। **করণীয়:** `cur`-এর সাথে `accs` লিস্টের কোনো আইটেম না মিললে placeholder prepend করো (অন্য সিলেক্টগুলোর প্যাটার্ন অনুসরণ করে), normal কেসে (account ঠিক থাকলে) আগের আচরণ অপরিবর্তিত রাখো।
12. **style.css — black/dark থিমে `.settings-desc.warn` লাল দেখায় না।** কারণ পাওয়া গেছে: লাইন ১০০ ও ৬৭৩-এ দুইবার `html[data-theme="dark"], html[data-theme="black"] .settings-desc{ color:#999; }` (৬৭৩-এরটা `.settings-desc.warn`-এর (লাইন ৩৯৬) পরে আসে ও সমান specificity বলে জিতে যায়, তাই warning টেক্সট লাল না হয়ে ধূসর দেখায়)। **করণীয়:** লাইন ৬৭৩-এর পরে একটা বেশি-স্পেসিফিক রুল যোগ করো, যেমন: `html[data-theme="dark"] .settings-desc.warn, html[data-theme="black"] .settings-desc.warn{ color: var(--ledger-red); }` (এবং চাইলে `.ok`-এর জন্যও একই প্যাটার্নে)।
13. **index.html — `#dashAccGrid .acc-card:nth-child(1..4)` CSS (লাইন ৩৬-৪৫ আশেপাশে) ডাইনামিক/কাস্টম অ্যাকাউন্টে ভাঙে।** সমস্যা: এই রঙগুলো `nth-child` পজিশনের ওপর নির্ভর করে বসানো, কিন্তু ইউজার অ্যাকাউন্ট আর্কাইভ/যোগ করলে পজিশন বদলে যায় — ভুল অ্যাকাউন্টে ভুল রঙ বসে যেতে পারে। **করণীয় (পছন্দমতো একটা):** (ক) JS-এ `renderDashAccCards`-এ (render.js) প্রতিটা কার্ডে `data-acc-idx` বা সরাসরি `style` দিয়ে রঙ বসাও (অ্যাকাউন্টের `id`/ইনডেক্স অনুযায়ী, `nth-child` না দিয়ে) — এটাই সবচেয়ে নির্ভরযোগ্য; অথবা (খ) প্রতিটা `DEFAULT_ACCOUNTS`/কাস্টম অ্যাকাউন্টে একটা স্থায়ী `iconBg` কালার ফিল্ড রাখো এবং CSS-এর বদলে render.js থেকেই সেই কালার বসাও। **নোট:** `style.css`-এর ৪৪২ লাইনের `#dashAccGrid .acc-card:last-child:nth-child(odd)` আলাদা ব্যাপার (গ্রিড লেআউট, রঙ না) — সেটা ছোঁয়ার দরকার নেই।
14. **sw.js — `install`-এ `c.addAll(FILES)` ফেল করলে silent।** (লাইন ৯-১১: `e.waitUntil(caches.open(CACHE).then(c => c.addAll(FILES)).then(() => self.skipWaiting()));`)। কোনো একটা ফাইল ৪০৪/নেটওয়ার্ক-এরর দিলে পুরো `addAll` reject করে, install ফেল করে, কিন্তু কোথাও `.catch()` নেই — silent ফেল, ইউজার/ডেভ কিছুই জানতে পারে না, PWA অফলাইন-ক্যাশ ছাড়াই থেকে যায়। **করণীয়:** `.catch(err => console.error('SW install failed', err))` যোগ করো অন্তত (silent থেকে visible করতে); চাইলে প্রতিটা ফাইল আলাদাভাবে `cache.add()` করে কোনটা ফেল করল লগ করাও যায় (ঐচ্ছিক, বেশি জটিল)।
15. **sw.js — `Response.error()` (লাইন ৩৫) খুব পুরনো ব্রাউজারে নেই।** ঝুঁকি কম (আধুনিক সব ব্রাউজারে সাপোর্টেড), কিন্তু ঠিক করতে চাইলে `Response.error()`-এর বদলে `new Response('', {status: 503, statusText: 'Offline'})` টাইপ কিছু ফলব্যাক ব্যবহার করা যায় যা সব ব্রাউজারে কাজ করবে।
16. **i18n.js — bn/en dict-এ কী-সংখ্যা সমান কিনা যাচাই করা হয়নি এই সেশনে।** এই সেশনে ২টা নতুন কী (`accBreakdownNote`) দুই ভাষাতেই যোগ করা হয়েছে বলে সমান থাকার কথা, কিন্তু নিশ্চিত করতে পরের সেশনে চালাও: `node -e "const {dict}=require('./i18n.js'); ..."` কাজ করবে না (browser script, module export নেই) — এর বদলে `grep -c ":" i18n.js` টাইপ কিছু বা `Object.keys(dict.bn).length === Object.keys(dict.en).length` চেক করার জন্য ফাইলটা সাময়িকভাবে node-এ eval করে একটা ছোট assert স্ক্রিপ্ট লেখো (আগের সেশনগুলোর প্যাটার্ন — T1-এর Progress Log দেখো)।
17. **HANDOFF.md ডকুমেন্টেশন-বাগ (এই ফাইলেরই):** এই ফাইলের §২-এর নিয়ম-৫ (উপরে লাইন ~৪৬) এ এখনও লেখা "বর্তমান `hisab-khata-v15`" — কিন্তু আসল `sw.js`-এ এখন `CACHE = 'hisab-khata-v18'`। এবং §১-এর ফাইল-টেবিলে (লাইন ~১৫-২৬) `privacy-lock.js` সারি নেই। **করণীয়:** লাইন ~৪৬-এ "v15" আপডেট করে আসল বর্তমান ভার্সন বসাও (T11 শেষে সেভ করার সময় যা হবে তাই লেখো, যেমন v19 যদি এই কাজেও `sw.js` বদলায়), আর ফাইল-টেবিলে `privacy-lock.js`-এর একটা সারি যোগ করো (কী আছে: App Lock — পাসওয়ার্ড/সিকিউরিটি-প্রশ্ন/fingerprint, `LK_*` কী-গুলো, `isLockEnabled`, `showLockScreen`)।

**T11-এর জন্য করণীয় (পরের সেশন):**
- উপরের ১১-১৭ ধারাক্রমে ঠিক করো (এই HANDOFF-এর নিয়ম অনুযায়ী একবারে একটা কাজ)।
- #১৩ বা #১৪/#১৫ কোনোটাই `sw.js`-এর `CACHE` বদলায় সেক্ষেত্রে ভার্সন বাড়াতে ভুলো না (এবং #১৭-তে সেই নতুন ভার্সনই লেখো)।
- সব `.js`-এ `node --check` চালিয়ে দেখো।
- এই সেশনে যা যা সম্পন্ন হয়েছে (উপরের ✅ ৯টা) তার কোনোটাই আসল ব্রাউজারে ক্লিক-থ্রু টেস্ট করা হয়নি (এই সেশনে ব্রাউজার/নেটওয়ার্ক অ্যাক্সেস ছিল না) — সময় পেলে Playwright দিয়ে অন্তত পাসওয়ার্ড-লক সেটআপ/চেঞ্জ ফ্লো (#২/#৩) এবং একটা এন্ট্রি/লোন/ডিউ ডিলিট ফ্লো (#৭) হাতেকলমে টেস্ট করে নিশ্চিত করাটা গুরুত্বপূর্ণ, যেহেতু এগুলো ডেটা-সুরক্ষা সংক্রান্ত।
**অ্যাক্সেপ্টেন্স (T11 সম্পূর্ণ হলে):** ১৭টার প্রতিটার পাশে DeepSeek-এর মূল বাংলা বর্ণনা মিলিয়ে দেখো — প্রতিটাই কোডে প্রতিফলিত হয়েছে কিনা। localStorage কোটা-ফুল সিমুলেট করে (`localStorage.setItem` ওভাররাইড করে throw করানো) পাসওয়ার্ড-সেটআপ, ব্যাকআপ-ইম্পোর্ট, এন্ট্রি/লোন/ডিউ-ডিলিট — প্রতিটাতে partial/মিশ্র অবস্থা তৈরি হয় না এটা কনফার্ম করো।


### [x] T12 — লক স্ক্রিনের আইকন: 🔒 → ফিঙ্গারপ্রিন্ট SVG *(মালিকের নতুন অনুরোধ, ২০২৬-০৯-২১)*
লক স্ক্রিনের ওপরের 🔒 ইমোজির জায়গায় প্রফেশনাল ফিঙ্গারপ্রিন্ট আইকন (ইনলাইন SVG, গোল ব্যাজে)। শুধু `index.html` (`.lock-icon`) ও `style.css` (`#appLockScreen .lock-icon`) বদলেছে; আইকনের রং `currentColor` = `var(--ledger-green)` তাই লাইট/ডার্ক/ব্ল্যাকে নিজে মানিয়ে যায়। সেটিংসের 🔒 ও "👆 ফিঙ্গারপ্রিন্ট দিয়ে আনলক করো" বাটন ছোঁয়া হয়নি।

---

## ৫. Progress Log (প্রতিটা সেশন শেষে নিচে যোগ করো)

| তারিখ | কাজ | কী বদলেছে / কোন ফাইল | বিচ্যুতি / পর্যবেক্ষণ | বাকি |
|---|---|---|---|---|
| 2026-09-20 | রিভিউ (কোড অপরিবর্তিত) | কিছু বদলায়নি। সব `.js` `node --check` পাস, i18n bn/en ৩৯২/৩৯২ মিলেছে, HTML-এ ডুপ্লিকেট id নেই | — | T1 থেকে শুরু |
| 2026-09-20 | T1 — দশমিক নির্ভুল করা | `state.js` (`round2`, `gtMoney`, `eqMoney` হেল্পার যোগ; `parseAmt` এখন `round2` রিটার্ন করে; `moneyFmt` নতুন — পূর্ণসংখ্যায় আগের মতো, দশমিক থাকলে ঠিক ২ ঘর দেখায়); `render.js` (`accountBalance`/`availableBalance` জমা `round2` দিয়ে; সব সামারি/লোন-ডিউ/চার্ট/নোট-প্ল্যান reduce `round2` দিয়ে মোড়ানো; সব "> accountBalance/avail" তুলনা `gtMoney` দিয়ে); `entries.js` (এন্ট্রি-ফর্ম ও এডিটের ব্যালেন্স-চেক `gtMoney`; অটো-সেভিংস পরিমাণ এখন `round2(amount*pct/100)`, আগে `Math.round`); `loans.js` (লোন/সেল্ফ-লোন/ডিপোজিট/উইথড্র/ট্রান্সফার/ডিউ-সেটল/লোন-সেটল/সেল্ফ-লোন-রিপে — সব ব্যালেন্স ও সেটল-এমাউন্ট তুলনা `gtMoney`; পুরনো `amt > x.amount + 0.01` টলারেন্স-হ্যাক সরিয়ে `gtMoney` দিয়ে প্রতিস্থাপন); `recurring.js` (পে-বাটনের ব্যালেন্স-চেক ও হিন্টের রং `gtMoney`); `notes-plans.js` (buy-plan এর দুটো ব্যালেন্স-চেক `gtMoney`); `sw.js` (`CACHE` ভার্সন `v6`→`v7`, কারণ উপরের ফাইলগুলো ক্যাশড এবং বদলেছে)। | কোড-লেভেলে কোনো বিচ্যুতি নেই। একটা পর্যবেক্ষণ (এই কাজের বাইরে, ভবিষ্যতে দেখার জন্য): `loans.js`-এর `deleteDue()` ফাংশনে একটা ব্যবহার-না-হওয়া `netChange` ভ্যারিয়েবল আছে (মৃত কোড, `round2` করিনি যেহেতু কোথাও ব্যবহৃতই হয় না — সম্ভবত T4/T5 এর কোনো অসমাপ্ত কাজের অবশিষ্টাংশ)। **যাচাই সততার সাথে:** এই কন্টেইনারে `jsdom` নেই এবং নেটওয়ার্ক বন্ধ, তাই পুরো অ্যাপ ব্রাউজার/DOM-এ চালিয়ে পরীক্ষা করা যায়নি। যা করা হয়েছে: (ক) সব `.js`-এ `node --check` পাস; (খ) `round2`/`gtMoney`/`eqMoney`/`moneyFmt`/`parseAmt`-এর প্রকৃত কোড state.js থেকে হুবহু কপি করে আলাদা node স্ক্রিপ্টে অ্যাসার্শন চালানো হয়েছে — ১০.১+২০.২ আয়ের পর ৩০.৩ খরচ আটকায় না, `moneyFmt(100.5)`→`৳১০০.৫০`, `moneyFmt(100)`→`৳১০০`, ইংরেজি লোকেলেও গ্রুপিং+দশমিক ঠিক, `gtMoney`/`eqMoney` ফ্লোট-ইম্প্রিসিশন ঠিকমতো সামলায় — সব পাস করেছে (দেখুন `/tmp/test_money.js`, এই সেশনেই তৈরি, রিপোতে সংরক্ষিত নেই); (গ) `diff` দিয়ে নিশ্চিত করা হয়েছে যে `i18n.js`, `index.html` অপরিবর্তিত আছে এবং বাকি ফাইলগুলোতে ঠিক প্ল্যান-করা লাইনগুলোই বদলেছে। যা করা **হয়নি**: ব্রাউজারে আসল ফর্ম পূরণ করে ক্লিক-থ্রু টেস্ট, এবং পুরনো (v1/v2) ব্যাকআপ ফাইল ইম্পোর্ট করে `loadData`/রেন্ডার ফ্লো বাস্তবে চালানো (এই টাস্কে ডেটা-লোড লজিক ছোঁয়া হয়নি বলে ঝুঁকি কম মনে হচ্ছে, কিন্তু চোখে-দেখা যাচাই করা হয়নি)। | — |
| 2026-09-20 | T2 — সেভ ফেল ও ডেটা সুরক্ষা | `state.js`: `safeSet(key,value)` (ফেলে `false` + লাল ব্যানার; নষ্ট কী-তে ব্লক) ও `safeSetJson`; সব `save*` এখন এগুলো দিয়ে চলে ও `true/false` রিটার্ন করে। `readStoredJson`+`quarantineCorrupt`: `loadData`-এর ৭টা কী (entries/notes/plans/loans/dues/recurring/settings) পার্স ফেল বা আকার ভুল (অ্যারে নয় / উপাদান অবজেক্ট নয়) হলে কাঁচা স্ট্রিং `hisab_corrupt_<key>_<timestamp>`-এ কপি (একই কপি থাকলে নতুন বানায় না, নইলে প্রতি রিলোডে জায়গা ভরত), ওই কী-তে সেভ ব্লক, ব্যানার। `requestPersistentStorage()` (`persisted()` আগে দেখে, না হলে `persist()`), `renderStoragePersistStatus()`, `openBackupSection()` (ব্যানারের বাটনগুলো এটাই খোলে)। `index.html`: `#storageFailBanner`, `#corruptDataBanner`, সেটিংসের ব্যাকআপ অংশে `#storagePersistStatus`। `style.css`: `.storage-banner` (লাইট/ডার্ক/ব্ল্যাক)। `i18n.js`: ১৭টা কী bn+en (৩৯২→৪০৯, দুটোই সমান)। `settings.js`: `hisab_lang` `safeSet` দিয়ে; `applyLanguage`/`openSettings`-এ নতুন রেন্ডার কল; ফ্যাক্টরি রিসেটে `hisab_corrupt_*` কপিও মোছে। `backup.js`: `hisab_lang` `safeSet` দিয়ে; `applyImportedBackup`-এ `clearCorruptBlocks()` এবং কোনো সেভ ফেল করলে "সফল" টোস্ট/রিলোড না দিয়ে ব্যর্থ টোস্ট। `init.js`: `requestPersistentStorage()` কল। `sw.js`: `CACHE` v7→v8। HANDOFF §১/§২-এর তথ্য (state.js সারি, i18n সংখ্যা ৪০৯, নতুন `hisab_corrupt_*` কী, CACHE নাম) কোডের সাথে মেলানো হয়েছে। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) স্পেকে "ইউজার সিদ্ধান্ত" কীভাবে নেবে বলা ছিল না; নষ্ট-ডেটা ব্যানারে ২ বাটন দিয়েছি — "ব্যাকআপ থেকে ফিরিয়ে আনো" (সেটিংসের ব্যাকআপ অংশে নেয়; ইম্পোর্ট হলে ব্লক ওঠে) ও "নতুন করে শুরু" (কনফার্মের পর ব্লক উঠে ফাঁকা ডেটা লেখা হয়)। কপি রাখা না গেলে (জায়গা নেই) কনফার্মে স্পষ্ট লেখে ডেটা চিরতরে যাবে। (২) লাল ব্যানার বন্ধ করা যায় না, রিলোড পর্যন্ত থাকে; পরে সেভ সফল হলেও লুকায় না ("স্থায়ী" পড়েছি)। (৩) স্পেকের বাইরে, তবে নতুন কী/রিটার্নের সরাসরি ফল: ফ্যাক্টরি রিসেটে corrupt কপি মোছা; `applyImportedBackup`-এ ফেল-চেক। (৪) `hisab_last_backup`/`hisab_backup_banner_dismissed` এখনও সরাসরি `localStorage` (ছোট মেটা ফ্ল্যাগ; স্পেকে শুধু `save*` ও `hisab_lang`)। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) `i18n.js:516` টপ-লেভেলে `localStorage.getItem('hisab_lang')` try/catch ছাড়া — ব্রাউজার localStorage অ্যাক্সেসই ব্লক করলে (SecurityError) অ্যাপ ব্যানারের আগেই ভাঙবে; `setItem` ফেল (কোটা ভরা) কভার হয়েছে, "অ্যাক্সেসই ব্লক" নয়। (খ) `hisab_corrupt_*` কপি ইউজার UI থেকে ডাউনলোড করা যায় না (শুধু devtools) — T3-তে "নষ্ট কপি ডাউনলোড" বিবেচনা করা যায়। (গ) কোনো কী নষ্ট/ব্লক থাকা অবস্থায় ব্যাকআপ এক্সপোর্ট করলে ওই অংশ ফাঁকা (মেমরির ডেটা) যাবে — T3-তে সতর্কতা দরকার; `applyImportedBackup` এখনও অ্যাটমিক নয় (একটা কী লেখা হয়ে আরেকটা ফেল করলে মিশ্র অবস্থা) — T3। (ঘ) `black` থিমে `.settings-desc.warn` লাল না হয়ে ধূসর দেখায় (`html[data-theme] .settings-desc` নির্দিষ্টতায় জেতে) — নতুন "স্টোরেজ সুরক্ষিত: না" ও বিদ্যমান ব্যাকআপ-স্ট্যাটাস দুটোতেই; UI পালিশে। (ঙ) T1-এর পর্যবেক্ষণ বহাল: `loans.js` `deleteDue()`-এর অব্যবহৃত `netChange`। **যাচাই সততার সাথে:** এবার আসল Chromium (Playwright, localhost সার্ভার, SW ব্লক করা) পাওয়া গেছে, তাই আসল UI-তে ৭৯টা চেক চালিয়েছি, সব পাস: `setItem` ফেল করালে ব্যানার আসে (একবারই, ভাষা বদলে অনুবাদ হয়, বাটন সেটিংসের ব্যাকআপ অংশ খোলে); নষ্ট `hisab_entries` (`{broken`, `[null]` ইত্যাদি ৫ রকম আকার) থাকলে আসল কী বাইট-বাই-বাইট অপরিবর্তিত থাকে এন্ট্রি যোগ ও রিলোডের পরেও, ঠিক ১টা কপি তৈরি হয়, অন্য (ভালো) কী স্বাভাবিক সেভ হয়; "নতুন করে শুরু", ব্যাকআপ (v1 ও এনক্রিপ্টেড v2) থেকে ফেরানো, ফ্যাক্টরি রিসেট, সেভ-ফেল অবস্থায় ইম্পোর্ট — সব পরীক্ষিত; `persist()` ৭ রকম (yes/no/API নেই/reject/sync throw…); ৩৮০px-এ লাইট/ডার্ক/ব্ল্যাক স্ক্রিনশট দেখা হয়েছে। **টেস্ট সংবেদনশীল কিনা:** একই নষ্ট-কী ও সেভ-ফেল পরিস্থিতি T1-পরবর্তী আসল কোডে চালিয়ে বাগ দুটোই পুনরুৎপাদিত হয়েছে (নষ্ট কী ওভাররাইট, কোনো ব্যানার/কপি নেই)। **পুরনো ডেটা:** legacy-আকারের ডেটা (transfer/budgetType/price ছাড়া, `entryId/entryId2`, রিকারিং `type:'transfer'`, `darkMode:'black'`) আসল ও নতুন কোডে লোড করে সব localStorage মান ও মেমরি-স্টেট **হুবহু এক**; ফাঁকা স্টোরেজে বুটও এক; T1-এর ১০.১+২০.২→৳৩০.৩ খরচ UI-তে এখনও পাস। **যা করা হয়নি:** আসল কোটা-ভরা অবস্থা (কেবল `setItem` throw করিয়ে সিমুলেট করেছি); আসল ফোন/Safari/iOS PWA; `persist()`-এর আসল ব্রাউজার-অনুমতি আচরণ (হেডলেস Chromium-এ ফল "না" এসেছে — এটা ব্রাউজারভেদে আলাদা); SW ক্যাশ বদল (SW ব্লক করে টেস্ট, `v8` শুধু কোড-পড়ে যাচাই)। | T3 থেকে শুরু |
| 2026-09-20 | T3 — ব্যাকআপ শক্তিশালী করা | `backup.js`: (১) এক্সপোর্ট মডালে দ্বিতীয় "পাসওয়ার্ড আবার লেখো" ইনপুট (শুধু export মোডে দেখায়; import মোডে লুকানো); খালি/৪ অক্ষরের কম/না-মেলা হলে মডালেই এরর, মডাল বন্ধ হয় না, ডাউনলোড হয় না; সংখ্যা গোনা হয় Unicode অক্ষরে (`Array.from`)। (২) নতুন `encryptAndVerifyBackup`: এনক্রিপ্টের পর মেমরিতে ডিক্রিপ্ট করে মূল ডেটার JSON-এর সাথে মেলায়; না মিললে বা throw করলে ডাউনলোড হয় না, বিদ্যমান "ব্যাকআপ তৈরি করা যায়নি" টোস্ট আসে। (৩) নতুন `analyzeImport` — ফাইল পার্সের পরেই, কিছু বদলানোর আগে, বাদ পড়া আইটেম গোনে (`applyImportedBackup` এখন এটাই ব্যবহার করে, তাই দুই জায়গার সংখ্যা সবসময় এক)। (৪) নতুন `startImportFlow`: কনফার্ম মেসেজে (সাধারণ + "N টা বাদ পড়বে, চালিয়ে যাবে?" যদি N>০ + "বর্তমান ডেটার কপি অটো-ডাউনলোড হবে, এনক্রিপ্ট করা নয়")। "হ্যাঁ" চাপলে `runImportWithSafetyCopy`: ওভাররাইটের আগের মেমরি-স্টেট থেকে `hisab-before-import-<date>.json` (সাধারণ, এনক্রিপ্ট নয়) ডাউনলোড; বানাতে exception হলে ইম্পোর্ট বাতিল ও টোস্ট; নইলে ৫০০ms পর ওভাররাইট। এনক্রিপ্টেড ও সাধারণ — দুই ইম্পোর্ট পথই এই ফ্লোতে। (৫) `applyImportedBackup`-এর অনুমোদিত `darkMode` তালিকায় `'black'`। (৬) `downloadBackupBlob(dataStr, filename)` — ঐচ্ছিক ফাইলনাম। `index.html`: `#backupPasswordConfirmInput` যোগ; পুরনো হার্ডকোড বাংলা placeholder "পাসওয়ার্ড (ঐচ্ছিক)" সরানো (এখন JS মোড অনুযায়ী i18n থেকে বসায়)। `i18n.js`: ৮টা কী bn+en (৪০৯→৪১৭, দুটোই সমান)। `sw.js`: `CACHE` v8→v9। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) নিরাপত্তা-কপি **সবসময়** নামে, এমনকি অ্যাপ ফাঁকা থাকলেও (নতুন ফোনে প্রথম রিস্টোরে একটা ফাঁকা "before-import" ফাইল নামবে) — স্পেকে শর্ত ছিল না বলে হুবহু মেনেছি; চাইলে "সব ফাঁকা হলে বাদ" শর্ত পরে দেওয়া যায়। (২) স্পেকের বাইরে ছোট বদল: ইম্পোর্ট মডালের ইনপুটে আগে সব মোডে "(ঐচ্ছিক)" লেখা ছিল (import-এ ভুল, কারণ পাসওয়ার্ড লাগেই) — এখন import মোডে শুধু "পাসওয়ার্ড"; হার্ডকোড বাংলা সরাতে বাধ্য ছিলাম (নিয়ম ৩)। (৩) ৪ অক্ষরের সীমা **শুধু এক্সপোর্টে**; ইম্পোর্টে কোনো সীমা নেই যেন ছোট পাসওয়ার্ডের পুরনো ব্যাকআপও খোলে। (৪) "পাসওয়ার্ড ছাড়াই ডাউনলোড" বাটন আগের মতোই (ভেরিফাই করার কিছু নেই)। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) T2-র (গ) বহাল: কোনো কী নষ্ট/ব্লক থাকলে এক্সপোর্ট ও নতুন নিরাপত্তা-কপি — দুটোই মেমরি থেকে বানানো, তাই ওই অংশ ফাঁকা যাবে (আসল নষ্ট ডেটা `hisab_corrupt_*`-এ থাকে); সতর্কতা দেখানো হয়নি (T3-র স্পেকে ছিল না)। (খ) `applyImportedBackup` এখনও অ্যাটমিক নয় (মাঝপথে সেভ ফেল করলে মিশ্র অবস্থা) — তবে এখন ইম্পোর্টের আগে নিরাপত্তা-কপি থাকে, ঝুঁকি কমেছে। (গ) T2-র (ঘ) নিশ্চিত: `black` থিমে `#backupPasswordError` (`.settings-desc.warn`) লাল না হয়ে ধূসর দেখায় — পড়া যায় কিন্তু জোরালো নয়; T8 বা UI পালিশে একটা CSS নিয়ম দিয়ে ঠিক হবে। (ঘ) **T8-র জন্য:** T8 শুধু `isValidImport*`-এর নিয়ম কড়া করলেই হবে — `analyzeImport` একই ফাংশন ধরে, তাই "বাদ পড়বে" গণনা আপনাআপনি মিলবে; আলাদা কিছু করতে হবে না। (ঙ) এনক্রিপ্টে এখন PBKDF2 (১,৫০,০০০ ধাপ) দুবার চলে (এনক্রিপ্ট + যাচাই); মডাল বন্ধ হওয়ার পর কোনো "অপেক্ষা করো" সংকেত নেই — ধীর ফোনে ডাউনলোডের আগে একটু বিরতি লাগতে পারে (মাপা হয়নি)। (চ) T1/T2-র পর্যবেক্ষণ (`deleteDue()`-এর অব্যবহৃত `netChange`; `i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া) বহাল। **যাচাই সততার সাথে:** আসল Chromium-এ (Playwright, localhost, SW ব্লক, ৩৮০px) ৪০টা চেক চালিয়েছি, সব পাস: ভুল/ছোট/খালি কনফার্ম পাসওয়ার্ডে কোনো ডাউনলোড হয় না ও মডাল খোলা থাকে; ঠিক ৪ অক্ষর + মিল → এনক্রিপ্টেড v2 ফাইল নামে (প্লেইনটেক্সট ফাঁস নেই); ডিক্রিপ্ট-যাচাই ফাঁকি দেওয়ালে (ভুল ডেটা ফেরত / throw) ডাউনলোড হয় না; সেই এনক্রিপ্টেড ফাইল ফাঁকা অ্যাপে আবার ইম্পোর্ট করলে ছয়টা ডেটা-কী ও `darkMode:'black'` হুবহু মেলে (তুলনা হয়েছে অ্যাপের নিজস্ব লোড-নর্মালাইজেশনের সাথে, কারণ অ্যাপ লোডেই নোটে `price:0`, লোনে `entryIds:[]` বসায় — এটা আগের কোডেও হয়); ভুল পাসওয়ার্ডে এরর ও কনফার্ম আসে না; ৪টা অবৈধ আইটেমওয়ালা v1 ফাইলে কনফার্মে "৪" ও "এনক্রিপ্ট করা নয়" দেখায়, কনফার্ম খোলা অবস্থায় কিছু ওভাররাইট হয় না, "না" চাপলে ডাউনলোড ও ডেটা-বদল শূন্য, "হ্যাঁ" চাপলে `hisab-before-import-<আজকের তারিখ>.json` নামে ওভাররাইটের **আগের** ৩টা এন্ট্রিসহ প্লেইন ফাইল নামে, তারপর ২টা বৈধ এন্ট্রি থাকে ও `black` টিকে থাকে; পুরনো `version:1` (settings/recurring/transfer-ফিল্ড ছাড়া) ফাইল ইম্পোর্ট চলে; ইংরেজি UI-র placeholder ও এরর; লাইট/ডার্ক/ব্ল্যাকে স্ক্রিনশট দেখা হয়েছে; কোনো JS error নেই; সব `.js`-এ `node --check` পাস; `diff`-এ শুধু ৪টা ফাইল বদলেছে। **টেস্ট সংবেদনশীল কিনা:** একই স্ক্রিপ্ট আগের কোডে চালালে ১০টা ফেল — কনফার্ম-ইনপুট নেই, ইম্পোর্টের আগে বাদ-পড়ার সংখ্যা নেই, নিরাপত্তা-কপি নামে না, `black` রিসেট হয়ে `system` হয়। **যা করা হয়নি:** আসল ফোন/iOS Safari PWA-তে ডাউনলোড আচরণ (বিশেষ করে একটা ডাউনলোডের ঠিক পরে ৫০০ms-এর মধ্যে রিলোড — Chromium-এ ঠিক ছিল, অন্য ব্রাউজারে দেখিনি); খুব বড় ডেটায় সময় মাপা; টেস্ট স্ক্রিপ্ট রিপোতে সংরক্ষিত নেই (এই সেশনের `/home/claude/t3_test.js`)। | T4 থেকে শুরু |
| 2026-09-20 | T4 — অ্যাকাউন্ট আর্কাইভ/ডিলিট | `state.js`: `getActiveAccountsList()` (আর্কাইভ বাদ), `getVisibleAccountsList()` (আর্কাইভ বাদ, তবে ব্যালেন্স ≠০ থাকলে থাকে), `accountUsage(id)` → `{entryCount, loans, dues, plans}` (এন্ট্রি গোনা হয় `account` দিয়ে, তাই `loanId/dueId`-যুক্ত এন্ট্রিও ধরা পড়ে; প্ল্যান = `account` বা `boughtAccount`), `accPlainName`/`accNameText` (প্লেইন নাম; আর্কাইভ হলে "(আর্কাইভ)" সহ), `setAccountSelectValue`; `accLabel` আর্কাইভ হলে "(আর্কাইভ)" জুড়ে দেয়; `getAccountsList()` এখনো **সব** ফেরত দেয় (লুকআপ/ফিল্টার/`savingsContributionByAccount`-এর জন্য)। `backup.js`: `normalizeAccount` শুধু `archived === true` (এবং সিস্টেম নয়) হলে `archived:true` টিকিয়ে রাখে, নইলে ফিল্ডই থাকে না (পুরনো ব্যাকআপ/ডেটার আকার অপরিবর্তিত); `migrateAccountsArray` এটা দিয়েই যায়। `render.js`: পুরনো `deleteAccountConfirm` (এন্ট্রিসহ মুছে দেওয়া) **সম্পূর্ণ সরানো**; নতুন `accountRowState` (system/delete/archive/transfer — D6 টেবিল) ও `accountAction` (ডিলিট/আর্কাইভ/ফেরানো/আগে-ট্রান্সফার; ক্লিকে ও কনফার্মে অবস্থা আবার যাচাই); `renderAccountsList` এখন সক্রিয় তালিকা + আলাদা "আর্কাইভ করা অ্যাকাউন্ট" অংশ (ফিরিয়ে আনো সহ); `renderAccountOptions` — লেনদেনের সব ড্রপডাউনে (১৪টা) শুধু সক্রিয়, ফিল্টার (`entryFilterAccount`, `allEntriesFilterAccount`) ও এডিট-মডালে আর্কাইভ সহ ("(আর্কাইভ)" লেবেলে); `renderDashAccCards`/`renderBalances`/ব্যালেন্স-ব্রেকডাউন `getVisibleAccountsList()` দিয়ে; নতুন অ্যাকাউন্টের নাম-ডুপ্লিকেট চেক আর্কাইভ করা নামও ধরে (আলাদা বার্তা); `renderAll()` এখন `renderAccountsList()`ও ডাকে (ভাষা বদলালে/আন্ডুতে সেটিংসের তালিকা মিলে থাকে)। `recurring.js`: পেন্ডিং-পে ড্রপডাউন `getActiveAccountsList()` থেকে। `entries.js`: এডিট মডালে অ্যাকাউন্ট ড্রপডাউনে না থাকলে (আর্কাইভ) অপশন যোগ করা হয় — নইলে সেভে অ্যাকাউন্ট `''` হয়ে যেত (আগে শুধু ট্রান্সফারে ছিল, এখন সব এন্ট্রিতে)। `loans.js` (লোন-সেটল ও সেল্ফ-লোন কার্ড-রিপে) ও `notes-plans.js` (প্ল্যান কেনা): ডিফল্ট অ্যাকাউন্ট বসানো `setAccountSelectValue` দিয়ে — লোন/প্ল্যান আর্কাইভ করা অ্যাকাউন্ট ধরে থাকলে আগের `select.value = ...` খালি হয়ে যেত ও লোন-আদায়ে `account:''` এন্ট্রি তৈরি হতো। `index.html`: `#archivedAccountsBox`/`#archivedAccountsList`। `style.css`: `.ar-btn.txt`, `.ar-btn.restore`। `i18n.js`: ১৯টা কী bn+en (৪১৭→৪৩৬, দুটোই সমান) — 'সিস্টেম', 'ডিলিট', সব টোস্ট/কনফার্ম/অ্যালার্ট বার্তা, (আর্কাইভ) সাফিক্স। `sw.js`: `CACHE` v9→v10। HANDOFF §১/§২ তথ্য কোডের সাথে মেলানো। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) `accountUsage` স্পেক অনুযায়ী ঠিক `{entryCount, loans, dues, plans}` — রিকারিং ধরা হয়নি (রিকারিং টেমপ্লেটে এখন `account` নেই)। ডিউ-তে বর্তমান ডেটা-মডেলে `account` ফিল্ডই নেই, তাই `dues` এখন সবসময় ০ (স্পেক মেনে রেখেছি, ভবিষ্যতে ফিল্ড এলে কাজ করবে)। (২) স্পেকের বাইরে, ডেটা-সুরক্ষার জন্য: (ক) `getVisibleAccountsList` — আর্কাইভ করা অ্যাকাউন্টে টাকা থেকে গেলে (এডভান্স মোডে ওই অ্যাকাউন্টের এন্ট্রি এডিট করলে, বা মোছার আন্ডু ফেরালে হতে পারে) সেটা ড্যাশবোর্ড ও মোটে থেকে যায়, নইলে টাকা "গায়েব" দেখাত; নতুন-এন্ট্রির ড্রপডাউনে তবু নেই, ফিরিয়ে আনতে হবে। (খ) সামারির অ্যাকাউন্ট-ব্রেকডাউনে আর্কাইভ করা অ্যাকাউন্ট শুধু সেই সময়ে এন্ট্রি থাকলে দেখায় (নইলে প্রতি মাসে "+৳০" সারি)। (গ) `entries.js`, `loans.js`, `notes-plans.js`-এ ছোট বদল — স্পেকে এগুলো তালিকায় ছিল না, কিন্তু না বদলালে আর্কাইভ চালুর পর ডেটা নষ্ট হতো (উপরে বিস্তারিত)। (৩) "আগে ট্রান্সফার করো" একটা টেক্সট-বাটন; চাপলে অ্যালার্ট বলে বর্তমান ব্যালেন্স কত ও ০ হতে হবে। ব্যালেন্স ঋণাত্মক হলেও একই বার্তা (স্পেকে আলাদা নিয়ম নেই)। (৪) "ডিলিট" এখন শুধু সম্পূর্ণ ফাঁকা অ্যাকাউন্টে; ট্রান্সফার-জোড়ার একপাশও একটা এন্ট্রি, তাই সেই অ্যাকাউন্ট ডিলিট নয়, আর্কাইভ — টাকা তৈরি/গায়েব হওয়ার মূল বাগ এভাবে বন্ধ। (৫) অ্যাকাউন্ট-ব্যবস্থাপনা অংশের **HTML-এর** স্থির বাংলা (শিরোনাম, বিবরণ, "+ নতুন অ্যাকাউন্ট", ফর্মের লেবেল/placeholder, বাতিল/সেভ) ছুঁইনি — স্পেকে তালিকাভুক্ত (সিস্টেম/ডিলিট/টোস্ট/কনফার্ম) সবই JS-এর ছিল; বাকিটা T8-এর "বাকি হার্ডকোড বাংলা"-য়। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) `renderAccountOptions`-এর `<option>` লেবেলে `a.icon + ' '` বসে, তাই বিকাশের আইকন-কী `'__bkash'` ড্রপডাউনে অক্ষর হিসেবে "__bkash বিকাশ" দেখায় — আগের (T3) কোডেও একই, দুই কোডে হুবহু এক পাওয়া গেছে; T8/UI পালিশে। (খ) T8-র `accLabelText` এখনো নেই: `accPlainName(a)` (অ্যাকাউন্ট-অবজেক্ট → প্লেইন নাম) আছে, কিন্তু `textContent`-এ বসা `accLabel(id)` এখনো escape-সহ ফেরত দেয় (ডাবল-escape বহাল) — T8 চাইলে `accPlainName(getAccountMeta(id))` ব্যবহার করতে পারে। (গ) T5-র জন্য: আর্কাইভ করা অ্যাকাউন্টের এন্ট্রি এডিট/আন্ডুতে তার ব্যালেন্স ০ থেকে সরতে পারে; `simulateBalances` শুধু "≥০ থেকে <০" ধরলে ০→ধনাত্মক আটকাবে না (ক্ষতিকর নয়), কিন্তু জেনে রাখা ভালো। (ঘ) নাম-ডুপ্লিকেট আর্কাইভ/সক্রিয় জোড়া শুধু ক্রাফটেড ইম্পোর্টে থাকতে পারে; "ফিরিয়ে আনো" তখন ডুপ্লিকেট আটকায় না। (ঙ) রিকারিংয়ের `history[*].account` শুধু মেটাডেটা, কোথাও দেখানো হয় না, তাই ব্যবহার-গণনায় ধরিনি; ডিলিট করা অ্যাকাউন্টের আইডি সেখানে থেকে গেলে ক্ষতি নেই। (চ) T1/T2/T3-র পর্যবেক্ষণ (`deleteDue()`-এর অব্যবহৃত `netChange`; `i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া; `black` থিমে `.settings-desc.warn` ধূসর; নষ্ট/ব্লক কী থাকলে এক্সপোর্ট ফাঁকা অংশ; `applyImportedBackup` অ্যাটমিক নয়) বহাল। **যাচাই সততার সাথে:** আসল Chromium-এ (Playwright, localhost, SW ব্লক, ৩৮০px) ৭৩টা চেক চালিয়েছি, সব পাস। (১) ৬১টা: D6 টেবিলের সব অবস্থা (ফাঁকা→🗑️; ব্যবহৃত+০→আর্কাইভ; ≠০→"আগে ট্রান্সফার করো" ও অ্যালার্টে ব্যালেন্স; লোন/প্ল্যান ধরা→ডিলিট নয়; সিস্টেম→বাটন নেই); ডিলিট/আর্কাইভ/ফেরানো আসল ক্লিকে; আর্কাইভের পর এন্ট্রি বাইট-বাই-বাইট অপরিবর্তিত; ১৪টা ড্রপডাউনে আর্কাইভ করা অ্যাকাউন্ট নেই; ড্যাশবোর্ড কার্ড সরে যায়/ফিরে আসে; ফিল্টার, এন্ট্রি-হিস্ট্রি ও সামারিতে "(আর্কাইভ)" (ইংরেজিতে "(archived)"); নাম-ডুপ্লিকেট (আর্কাইভ করা নামও); আর্কাইভ করা অ্যাকাউন্টের এন্ট্রি এডিটে অ্যাকাউন্ট খালি হয় না; লোন-সেটল/প্ল্যান-কেনায় ডিফল্ট অ্যাকাউন্ট খালি নয়; ব্যালেন্স ≠০ আর্কাইভ ড্যাশবোর্ডে থাকে ও মোটে ধরা হয়; সিস্টেম অ্যাকাউন্ট আর্কাইভ হয় না; ব্যাকআপ এক্সপোর্ট→ইম্পোর্ট (আসল UI ফ্লো) `archived` টিকিয়ে রাখে; পুরনো `version:1` ফাইল ইম্পোর্ট চলে; T1-এর দশমিক স্মোক (১০.১ ও ২০.২ আয়ের পর অটো-সেভিংস কেটে ঠিক ৳২৪.২৪ খরচ সফল); ক্লিকের মুহূর্তে ডেটা বদলে গেলে (stale) ডিলিট আটকায়; JS error নেই। (২) ১২টা: পুরনো-আকারের সমৃদ্ধ ডেটা (ট্রান্সফার-জোড়া, লোন-এন্ট্রি, `darkMode:'dark'`, প্ল্যান, লোন, ডিউ) T3 কোড ও নতুন কোডে লোড করে সব localStorage মান, ড্যাশবোর্ড, সামারি, এন্ট্রি-তালিকা, ১১টা ড্রপডাউন ও ব্যালেন্স-ব্রেকডাউন **হুবহু এক**; ট্রান্সফার-জোড়ার একপাশ থাকা অ্যাকাউন্ট আর্কাইভ করলে মোট ব্যালেন্স ও জোড়ার দুই পাশ অক্ষত। ইংরেজি UI-র বার্তাও দেখা হয়েছে; ৩৮০px-এ লাইট/ডার্ক/ব্ল্যাক স্ক্রিনশট চোখে দেখা। সব `.js`-এ `node --check` পাস; i18n bn/en ৪৩৬/৪৩৬; HTML-এ ডুপ্লিকেট id নেই; `diff`-এ শুধু ১১টা ফাইল বদলেছে (backup, entries, i18n, index.html, loans, notes-plans, recurring, render, state, style.css, sw)। **টেস্ট সংবেদনশীল কিনা:** একই স্ক্রিপ্ট T3 কোডে চালালে ফেল করে (আর্কাইভ/"আগে ট্রান্সফার"/ফিরিয়ে আনো নেই, পুরনো `deleteAccountConfirm` আছে, `accountUsage` নেই)। **যা করা হয়নি:** আসল ফোন/iOS Safari PWA; SW ক্যাশ বদল (SW ব্লক করে টেস্ট, `v10` শুধু কোড-পড়ে যাচাই); সেভ-ফেল (T2-র ব্যানার) অবস্থায় আর্কাইভ/ডিলিট; একাধিক ট্যাব একসাথে; মোছার আন্ডু-উইন্ডোর মধ্যে আর্কাইভ (শুধু কোড পড়ে ভেবেছি); টেস্ট স্ক্রিপ্ট রিপোতে সংরক্ষিত নেই (এই সেশনের `/home/claude/t4/`)। | T5 থেকে শুরু |
| 2026-09-20 | T5 — ডিলিট/এডিট নিয়ম ও অটো-সেভিংস সংযোগ | `entries.js`: আয়ের ফর্মে অটো-সেভিংসের ২টা এন্ট্রিতে `autoSavingsOf: <আয়ের id>` (আয়ের id আগে `nextId()` দিয়ে নেওয়া)। এডিট-সেভ পুরো নতুন: `applyEditPlan(list,id,plan)` — একই ফাংশন সিমুলেশন-কপি ও আসল `entries`-এ চলে; আয়ের পরিমাণ/তারিখ/অ্যাকাউন্ট বদলালে লিংকযুক্ত সেভিংস মিলে যায় (শতাংশ `getSavingsPctForMonth(নতুন তারিখের মাস)`, ব্যয়ের দিকের অ্যাকাউন্টও আয়ের নতুন অ্যাকাউন্টে যায়); আয়→ব্যয় করলে (বা নতুন মাসে সেভিংস % ০ হলে) সেভিংস মুছে যায় — কনফার্ম সহ; বদলের আগে `simulateBalances`, নেগেটিভ হলে অ্যালার্ট (কোন অ্যাকাউন্ট কত হবে) ও কিছুই বদলায় না। অটো-সেভিংস এন্ট্রি সরাসরি খুললে টোস্ট (`openEditEntry` + সেভ/ডিলিট হ্যান্ডলারেও গার্ড)। ডিলিট: `validateEntryDeletion(ids)` (লোন/ডিউ-যুক্ত নয়, অটো-সেভিংস আলাদা নয়, গ্রুপ সম্পূর্ণ — ট্রান্সফারের একপাশ/আয় একা মোছা যায় না, শেষ ৫টা কাজ, সিমুলেশনে নেগেটিভ নয়) বাটন-ক্লিক ও `scheduleEntryDeletion` (এখন `true/false` ফেরত) দুই জায়গাতেই; ডিলিট বাটন এডিট মডালে `canDeleteEntry` না হলে লুকানো; আয় মুছলে কনফার্মে অটো-সেভিংস পরিমাণসহ জানায়; আন্ডু একসাথে সব ফেরায় (বিদ্যমান ids-অ্যারে লজিক)। `render.js`: `isAutoSavingsEntry`, `linkedSavingsEntries`, `actionGroupKey`, `getRecentActionGroups(limit)` (id নতুন→পুরনো; আয়+অটো-সেভিংস=১, `pairId` জোড়া=১, বাকি ১টা করে; লোন/ডিউ এন্ট্রি গোনার বাইরে), `entryDeleteIds`, `canDeleteEntry`, `simulateBalances(changeFn)`, `simAccountList`। `loans.js`: `deleteLoan`/`deleteDue` মোছার আগে একই সিমুলেশন-চেক (নেগেটিভ হলে আটকায়); হার্ডকোড "টি লেনদেন এন্ট্রিও মুছে যাবে" → i18n `entriesAlsoDeletedMsg`। `i18n.js`: ৮টা কী bn+en (৪৩৬→৪৪৪, দুটোই সমান)। `sw.js`: `CACHE` v10→v11। HANDOFF §১/§২ তথ্য কোডের সাথে মেলানো। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) **"শেষ ৫টা কাজ" শুধু এডিট মোড চালু থাকলে** (D3-এর হুবহু কথা)। এডিট মোড বন্ধ থাকলে আগের মতো শুধু ১ ঘণ্টার গ্রেস পিরিয়ড — T5-এর করণীয়তে মোড-শর্ত লেখা ছিল না, D3-তে ছিল; চাইলে দুই মোডেই ৫টার সীমা দেওয়া এক লাইনের বদল (`canDeleteEntry`-এর `if(!settings.advancedMode) return true;`)। ব্যালেন্স-যাচাই কিন্তু **দুই মোডেই** চলে। (২) `simulateBalances` স্পেক অনুযায়ী শুধু "আগে ≥০ → পরে <০" ধরে; যে অ্যাকাউন্ট আগে থেকেই ঋণাত্মক (পুরনো ডেটায় সম্ভব) সেটা আরও খারাপ হলে আটকায় না। (৩) **এতিম লিংক:** `autoSavingsOf` যে আয়ের id ধরে সেটা এন্ট্রিতে না থাকলে (যেমন হাতে বানানো ইম্পোর্ট) `isAutoSavingsEntry` মিথ্যা — এন্ট্রিগুলো সাধারণ ট্রান্সফার-জোড়ার মতো এডিট/ডিলিট করা যায়, নইলে চিরতরে আটকে থাকত। (৪) নতুন মাসে সেভিংস % ০ হলে আয়ের এডিটে লিংকযুক্ত সেভিংস মুছে যায় (আয়→ব্যয়ের মতো কনফার্ম সহ) — স্পেকে সরাসরি বলা ছিল না, "সেভিংস মিলিয়ে দাও"-র স্বাভাবিক ফল ধরেছি। আগে লিংক না থাকা আয়ে (যেমন % ০ থাকতে তৈরি) এডিটে নতুন সেভিংস **তৈরি হয় না** (অনুমান নয়)। ব্যয়→আয় এডিটেও সেভিংস তৈরি হয় না। (৫) স্পেকের বাইরে ছোট: এডিট মডালের meta লাইনে লিংকযুক্ত আয়ে "অটো-সেভিংসও একসাথে বদলাবে" (`editLinkedSavingsMsg`)। (৬) T1-এর পর্যবেক্ষণের মৃত কোড `deleteDue()`-এর অব্যবহৃত `netChange` সরিয়েছি (ওই ফাংশন T5-তে বদলাতেই হচ্ছিল; কোথাও ব্যবহৃত ছিল না — `diff`-এ দেখা)। (৭) `deleteLoan`/`deleteDue`-এর সিমুলেশন-চেক কনফার্মের **আগে** — নেগেটিভ হলে কনফার্মই আসে না, শুধু অ্যালার্ট। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) লোন/ডিউ-যুক্ত এন্ট্রি "শেষ ৫টা কাজ" গণনার বাইরে (স্পেক অনুযায়ী) এবং সরাসরি এডিট/ডিলিট করা যায় না — শুধু `deleteLoan`/`deleteDue` দিয়ে যায়, সেখানে এখন ব্যালেন্স-চেক আছে। লোন/ডিউ ডিলিটের অনুমতি (`canManageLoansDues`) আগের মতোই, বদলাইনি। (খ) এডিট মডালের ওপর অ্যালার্ট/কনফার্ম খুললে মডাল পেছনে খোলা থাকে (ইচ্ছাকৃত, যাতে ইউজার পরিমাণ ঠিক করতে পারে); ডিলিটের বেলায় মডাল আগের মতো আগে বন্ধ হয়। (গ) T2/T3/T4-র পর্যবেক্ষণগুলো বহাল (`i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া; `black` থিমে `.settings-desc.warn` ধূসর; নষ্ট কী থাকলে এক্সপোর্ট ফাঁকা অংশ; `applyImportedBackup` অ্যাটমিক নয়; `'__bkash'` আইকন-কী ড্রপডাউনে অক্ষর; `accLabelText` T8 বাকি)। **যাচাই সততার সাথে:** আসল Chromium-এ (Playwright, localhost, SW ব্লক, ৩৮০px) ৬৮টা চেক চালিয়েছি, সব পাস। অ্যাক্সেপ্টেন্সের প্রতিটা: ১,০০০ আয় (২০% = ২০০ অটো, সেভিংস ও আয়ের এন্ট্রিতে `autoSavingsOf` ঠিক) → ৮০০ খরচ → আয় মুছতে গেলে আটকায় (অ্যালার্টে "নগদ (-৳২০০) নেগেটিভ… আগে খরচ মুছো"), কিছু মোছে না; খরচ মুছে আয় মুছলে আয়+২ সেভিংস একসাথে যায় (কনফার্মে ২০০ দেখায়); আন্ডু তিনটাই ফেরায় ও ব্যালেন্স আগের মতো; সব মুছে যাওয়া কাস্টম অ্যাকাউন্টে T4-র `accountRowState` = `delete`; আয় ১,০০০→৫০০ এডিটে দুই সেভিংস-এন্ট্রিই ২০০→১০০ ও ব্যালেন্স ঠিক; আয় কমালে ব্যালেন্স নেগেটিভ হবে এমন এডিট আটকায় ও কিছু বদলায় না; আয়ের তারিখ ১০%-সেভিংসের মাসে নিলে সেভিংস ৫০ ও তারিখ দুই এন্ট্রিতেই যায়; আয়→ব্যয় (অনুমোদিত হলে) কনফার্মের পর সেভিংস মুছে যায়, নেগেটিভ হলে কনফার্মে পৌঁছানোর আগেই আটকায়; শেষ ৫টার গ্রুপিং (আয়+সেভিংস=১, ট্রান্সফার-জোড়া=১, বাকি ৩টা — সীমানার ৫ম-তে বাটন আছে, ৬ষ্ঠে নেই); শেষ ৫টার বাইরের পুরনো এন্ট্রিতে ডিলিট নেই কিন্তু এডিট চলে (নেগেটিভ হলে আটকায়, নয়তো সফল); `scheduleEntryDeletion` সরাসরি কল করলেও গার্ড কাজ করে (অটো-সেভিংস আলাদা, আয় একা, ট্রান্সফারের একপাশ, শেষ ৫টার বাইরের — সব প্রত্যাখ্যাত); লিংকবিহীন পুরনো অটো-সেভিংস জোড়া অপরিবর্তিত ও সাধারণ ট্রান্সফারের মতো মোছা যায়, পুরনো আয়ের এডিট তাতে হাত দেয় না; এতিম লিংক আটকে থাকে না; `deleteLoan`/`deleteDue` নেগেটিভ হলে আটকায়, নয়তো নতুন i18n লাইনসহ কনফার্ম (বাংলা ও ইংরেজি); ইংরেজি UI-র বার্তা; `autoSavingsOf` রিলোড/নর্মালাইজেশনের পরও টিকে থাকে; JS error নেই; ৩৮০px-এ লাইট/ডার্ক/ব্ল্যাকে ৩টা নতুন অবস্থার স্ক্রিনশট (ডিলিট-লুকানো মডাল, নেগেটিভ-অ্যালার্ট, কনফার্ম) চোখে দেখা; সব `.js`-এ `node --check` পাস; i18n bn/en ৪৫২/৪৫২; HTML-এ ডুপ্লিকেট id নেই (HTML বদলায়নি); `diff`-এ শুধু ৫টা ফাইল বদলেছে (entries, i18n, loans, render, sw)। **টেস্ট সংবেদনশীল কিনা:** একই স্ক্রিপ্ট T4 কোডে চালালে প্রথম ধাপেই ফেল (`autoSavingsOf` নেই, আয় মোছা আটকায় না, meta-লাইন নেই)। **যা করা হয়নি:** আসল ফোন/iOS Safari PWA; SW ক্যাশ বদল (SW ব্লক করে টেস্ট, `v11` শুধু কোড-পড়ে যাচাই); আসল এক্সপোর্ট-ফাইল→ইম্পোর্ট UI ফ্লো দিয়ে `autoSavingsOf` টেকা (শুধু localStorage রিলোড ও `loadData` নর্মালাইজেশন দিয়ে দেখেছি; `backup.js` এন্ট্রির ফিল্ড হোয়াইটলিস্ট করে না — কোড পড়ে যাচাই, T8-এ ভ্যালিডেশন কড়া করার সময় `autoSavingsOf` (সসীম সংখ্যা বা অনুপস্থিত) বাদ না পড়ে খেয়াল রাখতে হবে); একাধিক ট্যাব; মোছার আন্ডু-উইন্ডোর মধ্যে আরেকটা এডিট/ডিলিট (আন্ডু `entries.splice(index)` — ব্যালেন্স আবার যাচাই হয় না, কোড পড়ে ভেবেছি); টেস্ট স্ক্রিপ্ট রিপোতে সংরক্ষিত নেই (এই সেশনের `/home/claude/t5/`)। | T6 থেকে শুরু |
| 2026-09-20 | T6 — সামারি ও রিকারিং | `state.js`: নতুন `isOperating(en)` (= `!transfer && !loanId && !dueId`); স্মার্ট বাজেট সাজেশনের ফিল্টার এখন এটা। `render.js`: `renderSummary`-এ নতুন `monthOperating` (= `monthNormal.filter(isOperating)`) — আয়, ব্যয়, `needSpent`/`wantSpent`, বাজেট টার্গেট (`inc × %`) ও খাত-ভিত্তিক ব্যয় এখন এটা থেকে; `computeChartData`-তে `isOperating`। `monthNormal` **আগের মতোই** থেকেছে (এন্ট্রি তালিকা ও "ঋণ আদায়/পরিশোধ" অংশ অপরিবর্তিত)। `recurring.js`: নতুন `recurringPayDate(tpl, period)` — চলতি পিরিয়ড → আজ; অতীতের মাস → ওই মাসের শেষ দিন (লিপ ইয়ার সহ); yearly অতীত → ৩১ ডিসেম্বর; `payRecurring` এটা ব্যবহার করে। `trimRecurringHistory` ও `RECURRING_HISTORY_CAP` **সম্পূর্ণ সরানো** (`payRecurring` ও `skipRecurring` থেকে কলও)। নতুন `detachRecurringHistoryForEntries(ids)` (মুছে যাওয়া এন্ট্রি `history[*].entryIds`-এ থাকলে ওই history রেকর্ড সরায়, সেভ করে, ব্যানার/টেমপ্লেট তালিকা রিফ্রেশ করে) ও `restoreRecurringHistory(removed)`। `entries.js`: `scheduleEntryDeletion` — গার্ড (`validateEntryDeletion`) পার হওয়ার পরই `detachRecurringHistoryForEntries` চলে (আটকানো ডিলিটে history ছোঁয়া হয় না); আন্ডু `restoreRecurringHistory` ডাকে। `sw.js`: `CACHE` v11→v12। `i18n.js`, `index.html`, `style.css` অপরিবর্তিত (নতুন কোনো ইউজার-টেক্সট নেই, তাই i18n এখনও ৪৫২/৪৫২)। HANDOFF §১/§২ (state/render/entries/recurring সারি, CACHE নাম) কোডের সাথে মেলানো। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) `accMonthBreakdown` (সামারির "অ্যাকাউন্ট অনুযায়ী মাসের নেট") ছুঁইনি — ওটা অ্যাকাউন্টে টাকার আসল নড়াচড়া দেখায় বলে লোন/ডিউ আদায়-পরিশোধও ধরে; স্পেকের তালিকায় (আয়/ব্যয়, বাজেট, চার্ট, স্মার্ট সাজেশন) ছিল না। সামারির আয়/ব্যয় কার্ডের যোগফলের সাথে ওই সারিগুলোর যোগফল তাই লোন/ডিউ থাকলে মিলবে না — এটা ইচ্ছাকৃত নয়, শুধু স্পেকের সীমা। (২) অতীত মাসের রিকারিং পে করলে এন্ট্রির তারিখ ওই মাসের শেষ দিন বসে, কিন্তু পেন্ডিং-মডালে ইউজারকে এটা জানানো হয় না (স্পেকে UI-বদল বা নতুন টেক্সট ছিল না, তাই যোগ করিনি)। চাইলে পেন্ডিং আইটেমে "তারিখ: ৩১ জানুয়ারি" লাইন যোগ করা যায়। (৩) `trimRecurringHistory` সরানোয় যাদের history আগেই কাটা পড়ে গেছে (৬০+ মাস পুরনো টেমপ্লেট) তাদের হারানো মাসগুলো ফিরবে না — আগে থেকেই pending দেখায়; এই কাজ সেটা ঠিক করে না (অ্যাপ নতুন, বাস্তবে এমন ডেটা থাকার সম্ভাবনা কম)। (৪) `restoreRecurringHistory` (আন্ডু) দুই ক্ষেত্রে কিছু ফেরায় না: এর মধ্যে টেমপ্লেট মুছে ফেললে; বা ৫ সেকেন্ডের আন্ডু-উইন্ডোর মধ্যে ওই মাস আবার পে/স্কিপ করলে (নতুন রেকর্ড ওভাররাইট করা হয় না)। পরের ক্ষেত্রে আন্ডুর পর এন্ট্রি ফিরে আসে কিন্তু মাসটা নতুন রেকর্ডের দিকে থাকে — মানে একই মাসের দুটো পেমেন্ট-এন্ট্রি থাকতে পারে; ভাবা হয়েছে, পরীক্ষা করিনি (বিরল)। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) রিকারিং পেমেন্টের এন্ট্রির পরিমাণ/তারিখ এডিট করলে `history[*].amount` পুরনোই থাকে (মেটাডেটা, কোথাও দেখানো হয় না)। (খ) D3 অনুযায়ী এডিট মোডে শুধু "শেষ ৫টা কাজ" মোছা যায়, তাই অনেক পুরনো রিকারিং পেমেন্ট মুছে মাস আবার pending করা যায় না — এই কাজের (ঘ) অংশ শুধু মোছা যায় এমন এন্ট্রির জন্য। (গ) **T8-র জন্য:** ইম্পোর্ট-ভ্যালিডেশন কড়া করার সময় এন্ট্রির `loanId`/`dueId` ও রিকারিং `history[*].entryIds` বাদ না পড়ে খেয়াল রাখতে হবে — `isOperating` ও নতুন history-সরানো এই ফিল্ডগুলোর উপর নির্ভর করে (`backup.js` এখন ফিল্ড হোয়াইটলিস্ট করে না, তাই সমস্যা নেই)। (ঘ) T1–T5-র পর্যবেক্ষণগুলো বহাল (`i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া; `black` থিমে `.settings-desc.warn` ধূসর; নষ্ট কী থাকলে এক্সপোর্ট ফাঁকা অংশ; `applyImportedBackup` অ্যাটমিক নয়; `'__bkash'` আইকন-কী ড্রপডাউনে অক্ষর; `accLabelText` T8 বাকি)। **যাচাই সততার সাথে:** আসল Chromium-এ (Playwright, localhost, SW ব্লক, ৩৮০px, ইংরেজি UI) ৩১টা চেক চালিয়েছি, সব পাস। **D7 (সামারি):** বেতন ৩০,০০০ + ২০,০০০ ধার আদায় (আসল `applyLoanPayment`) + ৪,০০০ ধার পরিশোধ + ৫,০০০ পাওনা আদায় + ১,০০০ দেনা পরিশোধ (আসল `applyDuePayment`) + ৩,০০০ ভাড়া → সামারির আয় ৳৩০,০০০, ব্যয় ৳৩,০০০ (আদায়-পরিশোধ বাদ); বাজেট টার্গেট ৩০,০০০-এর ৫০% = ৳১৫,০০০ (আগে ৳২৭,৫০০); খাত-তালিকায় শুধু "Rent"; "ঋণ আদায়/পরিশোধ" অংশে ২৫,০০০/৫,০০০ আগের মতো; মাসের এন্ট্রি তালিকায় ৬টাই (আদায়-পরিশোধ সহ) আগের মতো; চার্টের চলতি পিরিয়ড inc=৩০,০০০ exp=৩,০০০; নগদ ব্যালেন্সে আদায়-পরিশোধ এখনও ধরা (৩১,০০০ — ব্যালেন্স অপরিবর্তিত); স্মার্ট সাজেশন (গত মাসে আয় ১০,০০০ + ৩০,০০০ ধার আদায়, প্রয়োজনীয় ব্যয় ৬,০০০) → প্রয়োজন ৬০% (আগে ২০%)। **রিকারিং:** আসল UI-তে (ব্যানার → পেন্ডিং মডাল → পে বোতাম) ৩ মাস আগের মাসের ভাড়া পে করলে এন্ট্রির তারিখ ওই মাসের শেষ দিন ও ওই মাসের সামারিতে ৳৫,০০০ দেখায়; চলতি মাস → আজ; ২০২৪-০২ (লিপ) → ২০২৪-০২-২৯; yearly ২০২৪ → ২০২৪-১২-৩১, yearly চলতি বছর → আজ। ৮০ মাসের history (১টা pending) + পে বা স্কিপ (আসল কনফার্ম ক্লিক) → সব কী থাকে, pending ০, ব্যানার লুকায়, রিলোডের পরও ০ (আগে ২১টা পুরনো মাস pending হয়ে যেত)। **ডিলিট:** রিকারিং এন্ট্রি আসল UI-তে (এডিট মডাল → ডিলিট → কনফার্ম) মুছলে ওই টেমপ্লেটের ওই মাসের history সরে, মাস pending হয়, ব্যানার আসে, localStorage-এও সরে, অন্য টেমপ্লেট অক্ষত; আন্ডু বোতামে এন্ট্রি ও history হুবহু (JSON-এ এক) ফেরে, ব্যানার লুকায়; সম্পর্কহীন এন্ট্রি মুছলে history অক্ষত; নেগেটিভ ব্যালেন্সের কারণে আটকানো ডিলিটে এন্ট্রি ও history দুটোই অক্ষত। **পুরনো ডেটা:** legacy-আকার (`transfer`/`budgetType` ছাড়া এন্ট্রি, `entryId` লোন, রিকারিং `type:'transfer'` + `account`/`toAccount`, `darkMode:'black'`) T5 ও নতুন কোডে বুট করে সব localStorage মান, সামারি, তালিকা, খাত, বাজেট, ব্যালেন্স, pending সংখ্যা **হুবহু এক**, JS error নেই। **টেস্ট সংবেদনশীল কিনা:** একই স্ক্রিপ্ট T5 কোডে চালালে ৩১-এর মধ্যে ১৪টা ফেল (আয় ৳৫৫,০০০/ব্যয় ৳৮,০০০ দেখায়, বাজেট ৳২৭,৫০০, সাজেশন ২০%, অতীত মাসের পে আজকের তারিখে, লিপ/yearly তারিখ ভুল, history ৬০-এ কাটে ও ২১ মাস pending, মুছলে history থেকে যায়) — অর্থাৎ ছয়টা সমস্যার (ক)–(ঘ) সবই আসল কোডে পুনরুৎপাদিত। সব `.js`-এ `node --check` পাস; `diff`-এ শুধু ৫টা ফাইল বদলেছে (state, render, recurring, entries, sw)। **যা করা হয়নি:** আসল ফোন/iOS Safari PWA; SW ক্যাশ বদল (SW ব্লক করে টেস্ট, `v12` শুধু কোড-পড়ে যাচাই); T6-র জন্য নতুন স্ক্রিনশট চোখে দেখা হয়নি (HTML/CSS/i18n বদলায়নি, তাই দৃশ্য বদলার কথা নয় — তবু দেখা হয়নি); আন্ডু-উইন্ডোর মধ্যে টেমপ্লেট মোছা/মাস আবার পে করা (উপরে (৪)); একাধিক ট্যাব; আসল ব্যাকআপ এক্সপোর্ট→ইম্পোর্ট UI ফ্লো (T6 ব্যাকআপ কোড ছোঁয়নি); টেস্ট স্ক্রিপ্ট রিপোতে সংরক্ষিত নেই (এই সেশনের `/home/claude/t6/`)। | T7 থেকে শুরু |
| 2026-09-20 | T7 — তারিখ পুরনো থাকা ও কীবোর্ড/স্ক্রল | `state.js`: নতুন `lastKnownToday` (শুরুতে `new Date()`), `isSameDay(a,b)`, `refreshTodayDefaults(now)` (ডিফল্ট `new Date()`) — দিন একই হলে কিছু করে না; বদলে গেলে `setHeaderDate()`, তারপর `dateInput`/`loanDate`/`dueDate`/`selfLoanDate` — শুধু যেগুলোর মান এখনও *আগের* "আজ" (`toISO(prevToday)`)-এর সাথে মেলে সেগুলোই নতুন আজকের তারিখে বসায় (ইউজার হাতে অন্য তারিখ বসালে অক্ষত থাকে); `viewDay` একই নিয়মে (`isSameDay(viewDay, prevToday)`); `viewWeekStart` — `getWeekStart(prevToday)`-এর সাথে মিললে নতুন `getWeekStart(now)`; `viewMonth`/`viewYear` — আগের বছর+মাস মিললে নতুন মাসে; সবশেষে `renderAll()`। `init.js`: লোডের সময় `lastKnownToday = new Date()` (রিসেট, `loadData`/`applyLanguage` ইত্যাদির পর যেন সঠিক মুহূর্ত ধরে) এবং দুটো লিসেনার — `visibilitychange` (শুধু `visibilityState==='visible'` হলে) ও `window` এর `focus`, দুটোই `refreshTodayDefaults` কল করে। `settings.js`: (১) ডুপ্লিকেট `[role="button"][tabindex]` কীবোর্ড-হ্যান্ডলার (Enter/Space) সম্পূর্ণ সরানো — `entries.js`-এরটা (যেটা `role="button"` ও `role="tab"` দুটোই ধরে, HTML-এ ব্যবহৃত সব এলিমেন্টে `tabindex` আছে বলে সিলেক্টর মেলে) একাই রাখা হয়েছে। (২) Escape হ্যান্ডলার — আগে `!document.querySelector('.modal-overlay.open')` হলেই নিঃশর্তে `scrollLockCount = 0; unlockBodyScroll();` চলত (কোনো মডাল কখনো না খুললেও); এখন `scrollLockCount > 0 && কোনো মডাল খোলা নেই` হলেই কেবল, আর রিসেট করা হয় `while(scrollLockCount > 0) unlockBodyScroll();` দিয়ে (একটানে `=0` বসিয়ে জোর করে না, বরং আসল কাউন্ট যতবার লক হয়েছিল ততবার আনলক কল করে)। (৩) `unlockBodyScroll()`-এর শুরুতে গার্ড `if(scrollLockCount === 0) return;` — কল করার সময় কাউন্ট আগে থেকেই ০ থাকলে body-স্টাইল রিসেট বা `scrollTo` কিছুই হয় না। `sw.js`: `CACHE` v12→v13। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) `refreshTodayDefaults`/`lastKnownToday` `state.js`-এ রাখা হয়েছে (স্পেকে ফাইল নির্দিষ্ট করা ছিল না) — `viewDay`/`viewMonth`/`viewWeekStart`/`setHeaderDate` আগে থেকেই এখানে থাকায় স্বাভাবিক জায়গা মনে হয়েছে; লিসেনার-রেজিস্ট্রেশন `init.js`-এ (স্ক্রিপ্ট-লোড ক্রমে `state.js → … → init.js`, তাই `refreshTodayDefaults` ততক্ষণে সংজ্ঞায়িত)। (২) `refreshTodayDefaults` `periodMode` কোন ট্যাবে আছে তা দেখে না — দিন বদলালে `viewDay`/`viewWeekStart`/`viewMonth` তিনটাই (যেটা মেলে) আপডেট হয়, `periodMode` যেটাই থাকুক; ক্ষতিকর নয় (অদেখা মানও ঠিক থাকা ভালো), কিন্তু স্পেকে "যদি পুরনো আজ/চলতি সপ্তাহ/মাসে থাকে" ছাড়া `periodMode`-ভিত্তিক আলাদা শর্ত ছিল না বলে যোগ করিনি। (৩) `viewWeekStart` তুলনা `getWeekStart(prevToday)`-এর সাথে সমতা ধরে — ইউজার হাতে অন্য কোনো সপ্তাহে গিয়ে সেটা কাকতালীয়ভাবে "আগের চলতি সপ্তাহ"-এর সাথে মিলে গেলে (যেমন গত সপ্তাহে ব্রাউজ করছিল আর সেটাই এখন পুরনো-বর্তমান সপ্তাহ) সেটাও এগিয়ে যাবে — বিরল প্রান্তিক কেস, `viewMonth`/`viewDay`-তেও একই ধরনের সীমা আছে (স্পেকের ভাষাতেই "যদি পুরনো আজ/সপ্তাহ/মাসে থাকে" — ইউজার-নেভিগেশন আলাদাভাবে ট্র্যাক করার কথা বলা ছিল না)। (৪) `init.js`-এ `lastKnownToday = new Date()` আবার বসানো (যদিও `state.js`-এ ডিক্লেয়ার করার সময়ও বসে) — স্ক্রিপ্ট লোড ও `loadData()`/`applyLanguage()` চলতে কিছুটা সময় লাগলে সেই ফাঁক এড়াতে init-এর শেষে আবার নেওয়া (ক্ষতি নেই, শুধু বাড়তি নিশ্চয়তা)। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (ক) T1–T6-র পর্যবেক্ষণ সব বহাল (`deleteDue()`-এর অব্যবহৃত `netChange`; `i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া; `black` থিমে `.settings-desc.warn` ধূসর; নষ্ট/ব্লক কী থাকলে এক্সপোর্ট ফাঁকা অংশ; `applyImportedBackup` অ্যাটমিক নয়; `'__bkash'` আইকন-কী ড্রপডাউনে অক্ষর; `accLabelText` T8 বাকি; `accMonthBreakdown` লোন/ডিউ ধরে রাখে — T6)। (খ) নতুন কিছু ছোঁয়া হয়নি এই কাজে, শুধু উপরের তিনটা বাগ ঠিক করা হয়েছে। **যাচাই সততার সাথে:** এই সেশনে নেটওয়ার্ক বন্ধ ও কোনো ব্রাউজার/Playwright পাওয়া যায়নি (আগের সেশনগুলোর "আসল Chromium" পরীক্ষা এবার সম্ভব হয়নি) — তাই আসল UI-তে ক্লিক-থ্রু/স্ক্রিনশট দেখা **হয়নি**, যেটা আগের সেশনগুলোর তুলনায় যাচাইয়ের মান কমায়। যা করা হয়েছে: (ক) সব `.js`-এ `node --check` পাস; (খ) `refreshTodayDefaults`/`isSameDay`/`toISO`/`getWeekStart`-এর প্রকৃত কোড state.js থেকে হুবহু কপি করে DOM-স্টাব (fake `document.getElementById`, ফেক `renderAll`) দিয়ে আলাদা node স্ক্রিপ্টে লজিক টেস্ট করা হয়েছে — দিন বদলালে `dateInput` নতুন আজকে বসে কিন্তু হাতে-বদলানো `loanDate` অক্ষত থাকে; মাস-পরিবর্তন (৩০ সেপ্টেম্বর → ১ অক্টোবর) ঠিকমতো `viewMonth`/`viewYear` এগিয়ে দেয়; ইউজার আগে থেকেই অন্য মাসে (জুন) থাকলে `viewMonth` ছোঁয়া হয় না; একই দিনে দ্বিতীয়বার কল করলে কিছুই বদলায় না (`renderAll` আর কল হয় না) — সব পাস করেছে (দেখুন `/tmp/test_t7.js`, এই সেশনেই তৈরি, রিপোতে সংরক্ষিত নেই); (গ) স্ক্রল-লক ফিক্সের লজিকও আলাদাভাবে node-এ টেস্ট করা হয়েছে — কিছুই লক না থাকা অবস্থায় Escape চাপলে কোনো `scrollTo` হয় না (আগের বাগ: নিঃশর্তে হতো); ডাবল-ফায়ারের কারণে `scrollLockCount` আটকে থাকলে (২ লক, ১ আনলক → কাউন্ট ১-এ আটকে) Escape চাপলে পুরোপুরি আনলক হয় ও `scrollTo` ঠিক একবারই চলে (দেখুন `/tmp/test_scroll.js`) — পাস। (ঘ) `diff` দিয়ে নিশ্চিত করা হয়েছে `i18n.js`, `index.html`, `style.css`, `entries.js`, `backup.js`, `loans.js`, `recurring.js`, `render.js`, `notes-plans.js` অপরিবর্তিত আছে এবং বদলানো ৩টা ফাইলে (state.js, settings.js, init.js) শুধু প্ল্যান-করা লাইনগুলোই বদলেছে। **টেস্ট সংবেদনশীল কিনা:** যুক্তি দিয়ে যাচাই করা হয়েছে (ব্রাউজার না থাকায় "আগের কোডে চালিয়ে ফেল" literally করা যায়নি) — পুরনো Escape-কোড একই পরিস্থিতিতে (কিছু লক না থাকা অবস্থায়ও) `unlockBodyScroll()` কল করত যা `scrollLockCount = Math.max(0,-1)=0` ধরে বিনাশর্তে `scrollTo` চালাত, নতুন কোডে গার্ডের কারণে চলে না — এই পার্থক্যটাই মূল ফিক্স। **যা করা হয়নি:** আসল ব্রাউজার/Playwright-এ ক্লিক-থ্রু (visibilitychange ট্রিগার করে ট্যাব সুইচ, Enter/Space দিয়ে ডাবল-ফায়ার, Escape চেপে স্ক্রল-পজিশন চোখে দেখা) — এই সেশনে নেটওয়ার্ক/ব্রাউজার অ্যাক্সেস ছিল না; আসল ফোন/iOS Safari PWA (suspend থেকে ফিরলে `visibilitychange`/`focus` বাস্তবে ফায়ার করার আচরণ ব্রাউজারভেদে আলাদা হতে পারে, বিশেষ করে iOS Safari-তে); SW ক্যাশ বদল (SW ব্লক করে টেস্ট করার সুযোগ ছিল না, `v13` শুধু কোড-পড়ে যাচাই); `viewWeekStart`-এর প্রান্তিক কেস (বিচ্যুতি ৩) হাতে-কলমে দেখা; টেস্ট স্ক্রিপ্ট রিপোতে সংরক্ষিত নেই (এই সেশনের `/tmp/test_t7.js`, `/tmp/test_scroll.js`)। | T8 থেকে শুরু |
| 2026-09-20 | T8 — নিরাপত্তা ও ছোটখাটো | `backup.js`: ইম্পোর্ট ভ্যালিডেশন কড়া (আগের সেশনে), হারানো `filterAndCount` যোগ (এটা না থাকায় ইম্পোর্ট ReferenceError দিত), `normalizeAccount`-এ রঙ `safeCssColor` ও কাস্টম অ্যাকাউন্টে `i18n:false`। `settings.js`: `safeCssColor`, রিসেট-পাসওয়ার্ড কমেন্ট। `state.js`: `toEnglishDigits`, `accLabelText`, `accLabel`-এ i18n নামও escape, `attachAmtFormatting` কার্সর গোনায় বাংলা ডিজিট। `render.js`: অ্যাকাউন্ট নাম escape ও রঙ `safeCssColor`, Map/`Object.create(null)`, তারার `Math.max`। `index.html`: viewport থেকে `maximum-scale` সরানো; অ্যাকাউন্ট-ব্যবস্থাপনা অংশ ও রিকারিং placeholder `data-i18n`। `i18n.js`: ৮টা কী (৪৪৪→৪৫২)। `sw.js`: `res.ok && basic` ক্যাশ, navigate-only fallback, `CACHE` v14→v15। | **বিচ্যুতি:** স্পেকের বাইরে অ্যাকাউন্টের নাম/রঙ/`i18n` হার্ডেনিং (আমদানি করা অ্যাকাউন্টে XSS ফাঁক ছিল)। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** icon PNG (`icon-192/512/maskable-512.png`) মালিক পরে দিয়েছেন ও ফোল্ডারে যোগ হয়েছে (`sw.js` `FILES` ও `manifest.json` মেলে); `i18n.js` টপ-লেভেল `localStorage.getItem` try/catch ছাড়া; মডালের ভেতরে `touch-action:none/pan-y` (পিঞ্চ-জুম আটকায়, শুধু মডালে)। **যাচাই:** আসল Chromium (Playwright, ৩৮০px): ক্ষতিকর ব্যাকআপ UI-তে ইম্পোর্ট ২০/২০ পাস (১৫টা বাদ গোনা, নিরাপত্তা-কপি, স্ক্রিপ্ট চলেনি, `constructor`/`__proto__` নোট ঠিক গোনা); localStorage-এ সরাসরি ক্ষতিকর ডেটায় সব ট্যাব/মডাল ৬/৬ পাস; সব input ≥১৬px; `node --check` সব `.js` পাস। **করা হয়নি:** "১২৫০" টাইপ ও ভাষা-বদলে নতুন i18n টেক্সট চোখে দেখা, SW অফলাইন আচরণ, আসল ফোনে পিঞ্চ-জুম, পুরনো v1/v2 ব্যাকআপ ইম্পোর্ট (T8 কোডে), টেস্ট স্ক্রিপ্ট রিপোতে নেই। | T8 সম্পন্ন; T9/T10 মালিকের নতুন অনুরোধে যোগ হয়েছে |
| 2026-09-21 | T9 — ডার্ক মোড ডিফল্ট: লাইট | `state.js`: initial `settings` ভ্যারিয়েবলের `darkMode` `'system'`→`'light'`; `loadData`-এর ডিফল্ট অবজেক্টেও একই বদল; অবৈধ/নষ্ট `darkMode` মান পেলে ফলব্যাক `'system'`→`'light'`। `settings.js`: `applyDarkMode`-এর ডিফেন্সিভ ফলব্যাক (`(settings && settings.darkMode) \|\| 'system'`) → `\|\| 'light'`। "সিস্টেম" বাটন/অপশন ও `mode === 'system'` চেকগুলো (matchMedia লিসেনার সহ) অপরিবর্তিত রাখা হয়েছে — ইউজার এখনও "সিস্টেম" বেছে নিতে পারবে, শুধু প্রথমবারের ডিফল্ট বদলেছে। | **বিচ্যুতি নেই।** যা করা হয়েছে: (ক) সব `.js`-এ `node --check` পাস; (খ) `diff` দিয়ে নিশ্চিত `state.js`-এ ৩ লাইন ও `settings.js`-এ ১ লাইন ছাড়া কিছু বদলায়নি, বাকি সব ফাইল অপরিবর্তিত। **যা করা হয়নি (সততার সাথে):** এই সেশনে ব্রাউজার/Playwright পাওয়া যায়নি (নেটওয়ার্ক বন্ধ), তাই আসল UI-তে ফাঁকা localStorage-এ প্রথমবার খুলে থিম চোখে দেখা যায়নি — কোড-লেভেলে যুক্তি দিয়ে যাচাই করা হয়েছে (৪টা ফলব্যাক জায়গার প্রতিটাই খুঁজে বের করে বদলানো হয়েছে, `grep -n "'system'"` দিয়ে নিশ্চিত করা হয়েছে বাকি কোনো ডিফল্ট-ফলব্যাক বাদ পড়েনি — শুধু `mode === 'system'` লিটারেল-চেক দুটো (যেগুলো সঠিকভাবেই অপরিবর্তিত থাকা উচিত) বাকি আছে); পুরনো ইউজারের সেভ করা `darkMode:'system'`/`'dark'`/`'black'` রিলোডের পর অক্ষত থাকে কিনা আসল localStorage দিয়ে টেস্ট করা হয়নি (যুক্তি: `Object.assign(default, s\|\|{})`-এ `s` থেকে আসা মান সবসময় ডিফল্টকে override করে, তাই যুক্তিগতভাবে অক্ষত থাকার কথা)। | T10-এর সাথে একই সেশনে |
| 2026-09-21 | T10 — কোলাপ্সিবল প্যানেল অ্যাকর্ডিয়ন আচরণ | `render.js`: নতুন `closeSiblingCollapsibles(headerEl)` — headerEl-এর নিকটতম wrapper (`.settings-section`/`.settings-subsection`/`.card`)-এর প্যারেন্টের প্রত্যক্ষ চাইল্ড wrapper-গুলোর ভেতরে খোলা `.collapsible.open` হেডার ও `.collapse-body.open` বন্ধ করে (headerEl-এর নিজের wrapper বাদে); `toggleCollapse(headerEl, targetId)` এখন প্যানেল **খোলার** মুহূর্তে (আগে বন্ধ ছিল) এই ফাংশন কল করে, তারপর যথারীতি টগল করে। `notes-plans.js`: `togglePlanning()`-ও একই প্যাটার্নে `closeSiblingCollapsibles(toggleEl)` কল করে (এটা `toggleCollapse` ব্যবহার করে না, আলাদা ফাংশন, তাই আলাদাভাবে যোগ করা হয়েছে)। `sw.js`: `CACHE` v15→v16। | **বিচ্যুতি/সিদ্ধান্ত নেই — মালিকের সাথে আলোচনার পরে দুটো প্রশ্নের সিদ্ধান্ত HANDOFF-এই লেখা ছিল:** (১) গ্রুপিং শুধু সহোদর (sibling wrapper) পর্যায়ে, পুরো অ্যাপ-জুড়ে নয়; (২) নেস্টেড স্তরেও (এডভান্স মোডের ভেতরের সাব-সেকশন) একই নিয়ম, কিন্তু প্যারেন্ট-চাইল্ড সম্পর্ক স্পর্শ হয় না। **পর্যবেক্ষণ (ছোঁয়া হয়নি):** ভাষা-বদল/আন্ডু/`renderAll()`-এ DOM রি-রেন্ডার হলে `open` ক্লাসের অবস্থা কীভাবে সামলানো হয় তা এই টাস্কে বদলানো হয়নি (আগে যা হতো তাই হবে) — যাচাই করা হয়নি এতে নতুন কোনো অসামঞ্জস্য তৈরি হয় কিনা। কীবোর্ড (Enter/Space) দিয়ে ট্রিগার করলে ঠিক একই `toggleCollapse`/`togglePlanning` কল হয় (কোড পড়ে নিশ্চিত, `entries.js`-এর keydown হ্যান্ডলার `.click()` ডাকে), তাই আলাদা আচরণের কথা না, তবে আসল কীবোর্ডে চোখে দেখা হয়নি। **যাচাই সততার সাথে:** এই সেশনে নেটওয়ার্ক/ব্রাউজার অ্যাক্সেস ছিল না (আগের কিছু সেশনের মতো Playwright পাওয়া যায়নি), তাই আসল UI-তে ক্লিক-থ্রু/স্ক্রিনশট **হয়নি** — এটা আগের "আসল Chromium" যাচাইয়ের তুলনায় দুর্বল। যা করা হয়েছে: (ক) `closeSiblingCollapsibles`/`toggleCollapse`-এর প্রকৃত কোড `render.js` থেকে হুবহু বের করে (`eval` দিয়ে) একটা হালকা fake-DOM (নিজে-লেখা `classList`/`children`/`closest` স্টাব, jsdom ছাড়াই — কন্টেইনারে jsdom নেই) দিয়ে node-এ যুক্তি টেস্ট করা হয়েছে (`/tmp/t9t10test/test_accordion.js`, এই সেশনেই তৈরি, রিপোতে সংরক্ষিত নেই) — মালিকের স্ক্রিনশটের ৩-প্যানেল কেস (`panel-loans`-এর প্যাটার্ন) সিমুলেট করে: A খুলে B খুললে A বন্ধ, B খুলে C খুললে B বন্ধ, C-তে আবার ক্লিক করলে শুধু C বন্ধ (অন্য কিছু বদলায় না) — সব পাস; নেস্টেড সেটিংস কেসও সিমুলেট করে: টপ-লেভেল "ভাষা" খুলে "এডভান্স মোড" খুললে "ভাষা" বন্ধ হয়ে "এডভান্স মোড" খোলে, তারপর তার ভেতরের সাব-সেকশন ১ খুলে সাব-সেকশন ২ খুললে সাব-সেকশন ১ বন্ধ হয় কিন্তু বাইরের "এডভান্স মোড" (প্যারেন্ট) খোলাই থাকে — সব পাস। (খ) সব `.js`-এ `node --check` পাস। (গ) `diff` দিয়ে নিশ্চিত `render.js`, `notes-plans.js`, `sw.js` ছাড়া আর কিছু বদলায়নি। **যা করা হয়নি:** আসল Chromium/ফোনে `#panel-daily`, `#panel-loans`, `#panel-notes`, ও সেটিংস মডাল — এই ৪টা জায়গায় আসলেই ক্লিক করে চোখে দেখা (শুধু ফেক-DOM লজিক টেস্ট, আসল DOM/CSS/অ্যানিমেশনের সাথে না); ভাষা-বদল/আন্ডুর পর অবস্থা; SW ক্যাশ বদল (`v16` শুধু কোড-পড়ে যাচাই)। | পরের কোনো কাজ নেই — মালিক নতুন কাজ দিলে যোগ হবে |
| 2026-09-21 | T11 (আংশিক) — DeepSeek কোড-রিভিউ ফিক্স, ১-৯ সম্পন্ন | `i18n.js`: টপ-লেভেল `localStorage.getItem` try/catch। `privacy-lock.js`: সব সরাসরি `localStorage.setItem` → `safeSet`; পাসওয়ার্ড/সিকিউরিটি-প্রশ্ন সেভ হ্যান্ডলার try/catch + `lkCryptoAvailable()` গার্ড + সব-বা-কিছুই-না লেখা। `backup.js`: `applyImportedBackup` অ্যাটমিক (৮-কী রোলব্যাক + `loadData()`); `isValidImportRecurring`-এ `history` ঐচ্ছিক; `migrateAccountsArray`-এ নতুন `plansList` প্যারামিটার (সিগনেচার বদলেছে)। `state.js`: নতুন `atomicSaveKeys(keys, saveFns)` হেল্পার; `loadData`-এ `rmig` ফ্ল্যাগ (শুধু মাইগ্রেশন হলে `saveRecurring()`); `updateSettleAmtState`-এ `amt > max + 0.01` → `gtMoney(amt, max)`। `loans.js`: `deleteDue`/`deleteLoan` এখন `atomicSaveKeys` দিয়ে (in-memory রোলব্যাকসহ)। `entries.js`: `scheduleEntryDeletion` একইভাবে অ্যাটমিক। `recurring.js`: `detachRecurringHistoryForEntries`/`restoreRecurringHistory`-তে নতুন `save` প্যারামিটার (সিগনেচার বদলেছে)। `index.html`/`i18n.js`: `accMonthBreakdown`-এর নিচে ব্যাখ্যা-নোট (`accBreakdownNote`, bn+en)। **`sw.js` `CACHE` এই সেশনে বাড়ানো হয়নি (এখনও v18)** — কোনো ক্যাশড ফাইলের ইউজার-দৃশ্যমান আচরণ বদলায়নি ধরে নিয়ে (i18n/privacy-lock/backup/state/loans/entries/recurring/index.html সবই ক্যাশড ফাইল, কিন্তু পরিবর্তনগুলো মূলত এরর-হ্যান্ডলিং/অ্যাটমিসিটি — পরের সেশনে বিবেচনা করে দরকার মনে হলে v19 করো)। | **বিচ্যুতি/সিদ্ধান্ত (মালিক দেখে নিন):** (১) মালিক নিজে DeepSeek দিয়ে পাওয়া ১৭-পয়েন্ট তালিকা দিয়েছিলেন এই সেশনে — HANDOFF-এর কাজ-তালিকার বাইরের অনুরোধ, তাই নতুন T11 হিসেবে যোগ করা হলো (T9 নামটা আগে থেকেই ডার্ক-মোড-ডিফল্ট কাজে ব্যবহৃত, তাই কোড-কমেন্টে "T9" না লিখে "T11" লেখা হয়েছে — সবকটা কমেন্ট grep করে নিশ্চিত করা হয়েছে কোনো collision নেই)। (২) `migrateAccountsArray` ও `detachRecurringHistoryForEntries`/`restoreRecurringHistory`-এর সিগনেচার বদলেছে (নতুন প্যারামিটার) — এই তিনটার প্রতিটার **সব কলার এই সেশনে খুঁজে বের করে (`grep -rn`) আপডেট করা হয়েছে**, কিন্তু পরের সেশনে নতুন কোড যোগ করার সময় পুরনো সিগনেচারে কল না করে খেয়াল রাখতে হবে। (৩) `deleteDue`/`deleteLoan`/`scheduleEntryDeletion`-এর সেভ-ফেল-এ নতুন টোস্ট বার্তা `storageSaveFailMsg` ব্যবহার করা হয়েছে (আগে থেকেই i18n-এ ছিল, নতুন কী লাগেনি)। **পর্যবেক্ষণ (ছোঁয়া হয়নি, T11-এর বাকি অংশ):** #১১ `editAccount` সিলেক্টে placeholder নেই; #১২ black/dark থিমে `.settings-desc.warn` ধূসর (নির্দিষ্ট কারণ ও লাইন নম্বর উপরের T11 বিবরণে লেখা আছে); #১৩ `#dashAccGrid` nth-child CSS ডাইনামিক অ্যাকাউন্টে ভাঙে; #১৪ sw.js install-এর `addAll` silent-fail; #১৫ `Response.error()` পুরনো ব্রাউজারে নেই; #১৬ i18n bn/en কী-সংখ্যা এই সেশনে script দিয়ে verify করা হয়নি (শুধু নতুন ২টা কী দুই ভাষাতেই সমানভাবে যোগ করা হয়েছে); #১৭ এই HANDOFF.md ফাইলেই স্টেল `v15` উল্লেখ ও ফাইল-টেবিলে `privacy-lock.js` সারি অনুপস্থিত। এই ৭টার প্রতিটার জন্য ঠিক কী করতে হবে তার বিস্তারিত নির্দেশ উপরের T11 সেকশনে লেখা আছে। **যাচাই সততার সাথে:** এই সেশনে ব্রাউজার/নেটওয়ার্ক অ্যাক্সেস ছিল না, তাই **কোনো আসল UI ক্লিক-থ্রু টেস্ট হয়নি** — শুধু (ক) মূল zip খুলে প্রতিটা DeepSeek-দাবি কোড পড়ে সরাসরি যাচাই করা হয়েছে (গ্রেপ/ভিউ দিয়ে, অনুমান নয়); (খ) এডিট করা সব `.js` ফাইলে `node --check` (সিনট্যাক্স-শুধু, লজিক/DOM রান হয়নি) পাস করেছে। **এই সেশনের কাজ প্রোডাকশনে দেওয়ার আগে অন্তত একবার আসল ব্রাউজারে (বিশেষ করে পাসওয়ার্ড-লক সেটআপ ও এন্ট্রি/লোন/ডিউ ডিলিট ফ্লো) হাতে টেস্ট করা জরুরি**, যেহেতু এগুলো ডেটা-সুরক্ষা সংক্রান্ত পরিবর্তন। **যা করা হয়নি:** ১১-১৭ নম্বরের কোনো কোড-ফিক্স (শুধু ডায়াগনসিস ও করণীয় লেখা হয়েছে); `diff` দিয়ে ঠিক কোন কোন ফাইল/লাইন বদলেছে তার আলাদা তালিকা এই লগে জুড়ে দেওয়া হয়নি (কোড-কমেন্টে `T11:` মার্কার খুঁজলে (`grep -rn "T11:" *.js`) সবগুলো জায়গা পাওয়া যাবে)। | T11-এর ১১-১৭ নম্বর থেকে শুরু (উপরের বিস্তারিত করণীয় দেখো) |
| 2026-09-21 | T11 (আংশিক, ২য় সেশন) — ১১-১৬ সম্পন্ন, ১৭ ও privacy-lock বাগ বাকি | `render.js` (`renderAccountOptions`: `editAccount`-এ cur না মিললে placeholder — #১১); `entries.js` (এডিট মডালে placeholder সবসময় disabled; অ্যাকাউন্ট খালি থাকলে সেভ আটকায়, `entrySelectAccountMsg` টোস্ট — #১১); `style.css` (`html[data-theme] .settings-desc.warn/.ok` বেশি-স্পেসিফিক রুল — #১২); `index.html` (মৃত `#dashAccGrid .acc-card:nth-child(N) .acc-ic` CSS সরানো — #১৩); `sw.js` (`install`-এ `.catch` — লগ করে আবার throw; `Response.error()` → 503 `Response`; `CACHE` v18→v19 — #১৪/#১৫)। #১৬: i18n bn/en ৫১১/৫১১, ব্যবহৃত সব কী দুই ভাষায় আছে। | **পর্যবেক্ষণ/বিচ্যুতি:** (১) #১২ বাগ শুধু black থিমে ছিল, dark-এ নয় (`html[data-theme=dark], html[data-theme=black] .settings-desc` সিলেক্টর-লিস্টের প্রথম অংশ `html`-কেই ধরে)। (২) #১৩: কোনো এলিমেন্টেই `.acc-ic` ক্লাস নেই, ফলে ওই CSS আগে থেকেই অকার্যকর ছিল; আইকন-রং আগে থেকেই id-ভিত্তিক inline — দৃশ্যে বদল নেই। (৩) #১৪-এ HANDOFF-এর প্রস্তাবিত `.catch(console.error)` হুবহু দিইনি — এরর গিলে ফেললে অর্ধেক-ক্যাশ নিয়ে নতুন SW সক্রিয় হয়ে পুরনো ক্যাশ মুছত, তাই লগ করে আবার throw। (৪) HANDOFF §১-এ i18n সংখ্যা ৪৫২/৪৫২ পুরনো — এখন ৫১১/৫১১। **🔴 নতুন বাগ (ছোঁয়া হয়নি):** `privacy-lock.js` পাসওয়ার্ড **change/reset**-এ hash সেভের পর salt সেভ ফেল করলে `removeItem(LK_PW_HASH); removeItem(LK_PW_SALT)` চলে — পুরনো বৈধ পাসওয়ার্ডও মুছে যায়, `LK_ENABLED` '1' থেকে যায় (আসল Chromium-এ পুনরুৎপাদিত: hash/salt দুটোই `<gone>`)। এছাড়া এই ফেলে ইউজারকে ভুল বার্তা দেখায় (`lockPwTooShortErr` — "কমপক্ষে ৪ অক্ষর"); `storageSaveFailMsg` হওয়া উচিত। ঠিক করার পথ: লেখার আগে পুরনো hash/salt পড়ে রাখো, ফেল করলে সেগুলো ফেরাও (শুধু `setup` মোডে মোছা ঠিক); একই প্যাটার্ন security-question সেভ হ্যান্ডলারেও দেখো। **যাচাই সততার সাথে:** আসল Chromium (Playwright, ৩৮০px) — #১১ পুরনো কোডে বাগ পুনরুৎপাদিত, নতুনে পাস; #১২ black-এ পুরনোতে ধূসর, নতুনে লাল; #১৩ পুরনো-নতুন দুটোতেই `.acc-ic` ০টি; SW: stub-এ ৬/৬ (পুরনোতে ২টা ফেল), আসল Chromium-এ install+activate হয়ে ১৮টা ফাইল v19-এ ক্যাশ। **করা হয়নি:** Playwright-এর অফলাইন মোড SW-এর অনুরোধ আটকায় না, তাই অফলাইন শাখা আসল ব্রাউজারে দেখা হয়নি; ব্যাকআপ-ইম্পোর্ট অ্যাটমিক ও এন্ট্রি/লোন/ডিউ ডিলিটের সেভ-ফেল টেস্ট (T11 অ্যাক্সেপ্টেন্স) চালানো হয়নি; আসল ফোন/iOS; টেস্ট স্ক্রিপ্ট রিপোতে নেই। | **#১৭** (HANDOFF ডক: §২ নিয়ম-৫-এর `v15` → `v19`, §১ ফাইল-টেবিলে `privacy-lock.js` সারি ও `hisab_lock_*` কী, স্ক্রিপ্ট-ক্রমে `privacy-lock.js`, i18n সংখ্যা ৫১১); `privacy-lock.js` বাগ ঠিক; T11 অ্যাক্সেপ্টেন্সের বাকি টেস্ট; তারপর T11 `[x]` |
| 2026-09-21 | T11 (৩য় সেশন) — সম্পন্ন: #১৭ + privacy-lock বাগ + অ্যাক্সেপ্টেন্স টেস্ট | `privacy-lock.js`: নতুন `lkSnapshotKeys`/`lkRestoreKeys`; পাসওয়ার্ড সেভ (setup/change/reset) ও সিকিউরিটি-প্রশ্ন সেভে লেখার আগে পুরনো মান নিয়ে রাখা, ফেল হলে সেই মানে ফেরানো (আগে `removeItem` করত — change/reset-এ পুরনো বৈধ পাসওয়ার্ড মুছে যেত, `LK_ENABLED` থেকে যেত); setup-এ `LK_ENABLED` লেখা ফেল হলে hash/salt-ও ফেরানো; ফেলের বার্তা এখন `storageSaveFailMsg` (আগে ভুল করে `lockPwTooShortErr`/`securityQRequiredErr`)। `sw.js`: `CACHE` v19→v20। `HANDOFF.md`: #১৭ — §২ নিয়ম-৫ `v20`, §১ টেবিলে `privacy-lock.js` সারি + `atomicSaveKeys` উল্লেখ, স্ক্রিপ্ট-ক্রমে `privacy-lock.js`, `hisab_lock_*` কী, i18n ৫১১/৫১১; T11 `[x]`। | **পর্যবেক্ষণ (ছোঁয়া হয়নি):** (১) অচেনা অ্যাকাউন্ট আইডি এডিট ড্রপডাউনে/সব জায়গায় "নগদ" লেবেলে দেখায় (`accLabelText` ফলব্যাক `accCash`) — আগে থেকেই। (২) আন্ডু চলাকালীন সেভ ফেল হলে মেমরিতে এন্ট্রি ফেরে কিন্তু localStorage আগের (মোছা) অবস্থায় থাকে — পরের সফল সেভে মিলে যায়। (৩) ব্যাকআপ ইম্পোর্টের ডিফল্ট `darkMode` এখনও `'system'` (`backup.js`), T9-এর সাথে অসংগত — মালিক ঠিক করুন। (৪) `privacy-lock.js`-এর কাউন্টার/কুলডাউন/last-activity লেখা এখনও সরাসরি `localStorage` (try/catch সহ, ছোট মেটা)। **যাচাই সততার সাথে:** আসল Chromium (Playwright, ৩৮০px, SW ব্লক): লক-রোলব্যাক ২০/২০ (পুরনো কোডে ৯/২০ — বাগ পুনরুৎপাদিত); `setItem` throw করিয়ে এন্ট্রি/লোন/ডিউ ডিলিট (রিকারিং history সহ) ও ব্যাকআপ ইম্পোর্ট (শেষ/মাঝের কী ফেল) ৩৫/৩৫ — রোলব্যাক ইচ্ছে করে বন্ধ করলে ১৪টা ফেল; #১১–#১৫ ১২/১২ (আসল SW install, ১৮ ফাইল v20-এ ক্যাশ); পাসওয়ার্ড বদলের পর রিলোডে নতুন পাসওয়ার্ডে আনলক ৪/৪; `version:1` ব্যাকআপে `history`-ছাড়া রিকারিং ইম্পোর্ট, ট্যাব/সেটিংস স্মোক ৫/৫; i18n ৫১১/৫১১ ও ব্যবহৃত ৪৬৪ কী দুই ভাষায় আছে; সব `.js`-এ `node --check` পাস। **করা হয়নি:** আসল ফোন/iOS Safari PWA; SW-এর অফলাইন শাখা; আসল কোটা-ভরা অবস্থা (শুধু `setItem` throw সিমুলেশন); DeepSeek-এর মূল বাংলা বর্ণনা হাতে ছিল না — HANDOFF-এর বিবরণের সাথে মিলিয়েছি; টেস্ট স্ক্রিপ্ট রিপোতে নেই (সেশনের `/home/claude/t11/`)। | পরের কোনো কাজ নেই — মালিক নতুন কাজ দিলে যোগ হবে |
| 2026-09-21 | T12 — লক স্ক্রিনে ফিঙ্গারপ্রিন্ট আইকন | `index.html` (`.lock-icon`: 🔒 → ইনলাইন SVG, `aria-hidden`); `style.css` (`.lock-icon` এখন ৮৪px গোল ব্যাজ, `color:var(--ledger-green)`, `background:var(--paper)`, বর্ডার `var(--line)`); `sw.js` `CACHE` v20→v21। | বিচ্যুতি নেই। পর্যবেক্ষণ (ছোঁয়া হয়নি): লক স্ক্রিনের বাটনে এখনও 👆 ইমোজি আছে (`lockScreenFingerprintBtn`, i18n); সেটিংসের প্রাইভেসি হেডারে 🔒 আছে। **যাচাই:** আসল Chromium (Playwright, ৩৮০px, SW ব্লক) — লাইট/ডার্ক/ব্ল্যাকে লক স্ক্রিন খুলে স্ক্রিনশট দেখা, ব্যাজ ৮৪×৮৪, JS error নেই; `node --check` পাস। **করা হয়নি:** আসল ফোন। | পরের কোনো কাজ নেই |
---

## ৬. পরের সেশনের জন্য দ্রুত পরীক্ষা (কাজ শুরুর আগে)

- `for f in *.js; do node --check $f; done` পাস করছে কি?
- `sw.js`-এর `CACHE` ভার্সন কত? (আগের সেশনে বাড়ানো হয়েছে কি?)
- Progress Log-এর শেষ সারির "বাকি" কলাম দেখো।
